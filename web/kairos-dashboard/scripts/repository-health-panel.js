import{getRepositoryHealth}from'./repository-health-engine.js';function e(v){return String(v||'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-repository-health-panel]'))return;const h=getRepositoryHealth();const card=document.createElement('article');card.className='card full';card.dataset.repositoryHealthPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Repository</p><h3>Repository Health</h3></div><span class="${h.healthy?'badge good':'badge warning'}">${e(h.status)}</span></div><p class="muted" style="margin-top:12px;">${e(h.summary)} • ${e(h.lastSync)}</p>`;v.prepend(card)}window.addEventListener('DOMContentLoaded',render);window.addEventListener('kairos:auth',render);