import{getSystemHealthSummary}from'./system-health-summary.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-system-health-panel]'))return;const s=getSystemHealthSummary();const card=document.createElement('article');card.className='card full';card.dataset.systemHealthPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">System</p><h3>System Health Summary</h3></div><span class="badge good">${e(s.score)}%</span></div><div class="list" style="margin-top:16px;"><div class="list-item"><div><strong>Status</strong><p class="muted">${e(s.status)} • ${e(s.updated)}</p></div><span class="badge good">Health</span></div>${s.highlights.map(i=>`<div class="list-item"><div><strong>${e(i.title)}</strong><p class="muted">${e(i.value)}</p></div><span class="badge">Signal</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);