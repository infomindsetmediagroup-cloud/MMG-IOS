const BUILD="kairos-visitor-activity-ui-20260713-1";const state={open:false,loading:false,error:"",report:null,workflow:null};start();function start(){document.addEventListener("click",intercept,true);window.addEventListener("kairos:visitor-activity:open",openWorkspace)}function intercept(event){const button=event.target.closest?.('[data-child="visitor-activity"]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();openWorkspace()}async function openWorkspace(){state.open=true;await loadLatest();render();setTimeout(()=>document.querySelector("#visitor-activity")?.scrollIntoView({behavior:"smooth",block:"start"}),20)}async function loadLatest(){try{const{response,body}=await request("/api/visitor-activity/latest");if(response.ok)state.report=body.report||null}catch{}}async function runReview(event){event.preventDefault();const data=new FormData(event.currentTarget);state.loading=true;state.error="";render();try{const{response,body}=await request("/api/visitor-activity/reviews",{method:"POST",headers:headers(),body:JSON.stringify({objective:data.get("objective"),period:data.get("period"),priority:data.get("priority"),createWorkflow:data.get("createWorkflow")==="on"})});if(!response.ok)throw new Error(body?.error?.message||"Kairos could not complete the visitor review.");state.report=body.report;state.workflow=body.workflow||null}catch(error){state.error=error.message||"Kairos could not complete the visitor review."}finally{state.loading=false;render()}}function render(){const hub=document.querySelector("#kairos-hub");if(!hub)return;let root=document.querySelector("#visitor-activity");if(!state.open){root?.remove();return}if(!root){root=document.createElement("section");root.id="visitor-activity";root.className="visitor-activity workspace";hub.appendChild(root)}root.innerHTML=`<header class="visitor-head"><div><p class="eyebrow">Customers · Visitor Activity</p><h2>Verified Visitor Evidence</h2><p>Review aggregate Shopify visitor signals without identifying or profiling individual people.</p></div><button type="button" data-close-visitor>Close</button></header>${state.error?`<p class="visitor-error">${escapeHTML(state.error)}</p>`:""}<div class="visitor-layout"><section class="visitor-form"><h3>Run Visitor Review</h3><form data-visitor-form><label>Review objective<textarea name="objective" maxlength="2400" required placeholder="Example: Review current visitor behavior and identify one customer-journey improvement."></textarea></label><label>Requested period<input name="period" maxlength="180" value="current verified snapshot"></label><label>Priority<select name="priority"><option value="high">High</option><option value="normal" selected>Normal</option><option value="critical">Critical</option><option value="low">Low</option></select></label><label class="visitor-check"><input type="checkbox" name="createWorkflow" checked> Create a five-task follow-through workflow</label><button class="primary" type="submit">Run Verified Review</button></form><div class="visitor-privacy"><strong>Privacy boundary</strong><p>Aggregate evidence only. No individual visitor identification, inferred identity, or personal profiling.</p></div></section><section class="visitor-report">${state.loading?`<p class="visitor-loading">Kairos is reading the authoritative visitor snapshot…</p>`:reportMarkup()}</section></div>`;bind()}function reportMarkup(){const report=state.report;if(!report)return`<div class="visitor-empty"><strong>No verified visitor review yet.</strong><p>Run the first review to capture the current aggregate evidence.</p></div>`;const metrics=Array.isArray(report.metrics)?report.metrics:[];const findings=Array.isArray(report.findings)?report.findings:[];const actions=Array.isArray(report.nextActions)?report.nextActions:[];return`<article class="visitor-review"><div class="visitor-report-head"><div><p class="eyebrow">${escapeHTML(report.status)}</p><h3>${escapeHTML(report.requestedPeriod)}</h3></div><span>${escapeHTML(report.coverage?.label||"Verified snapshot")}</span></div><p class="visitor-qualification">${escapeHTML(report.coverage?.qualification||"")}</p><div class="visitor-metrics">${metrics.length?metrics.map(m=>`<div><span>${escapeHTML(m.label)}</span><strong>${escapeHTML(m.displayValue)}</strong><small>${escapeHTML(m.source)}</small></div>`).join(""):`<div class="visitor-no-metrics"><strong>Visitor metrics unavailable</strong><span>Review the coverage findings below.</span></div>`}</div><section class="visitor-findings"><h4>Verified findings</h4>${findings.map(item=>`<article data-severity="${escapeHTML(item.severity)}"><strong>${escapeHTML(item.title)}</strong><p>${escapeHTML(item.detail)}</p></article>`).join("")}</section><section class="visitor-actions"><h4>Next bounded actions</h4><ol>${actions.map(action=>`<li>${escapeHTML(action)}</li>`).join("")}</ol></section><footer><span>Aggregate evidence only</span><span>No inferred identity</span><span>No profiling</span></footer>${state.workflow?`<button type="button" data-open-visitor-workflow>Open Follow-Through Workflow</button>`:""}</article>`}function bind(){document.querySelector("[data-close-visitor]")?.addEventListener("click",()=>{state.open=false;render()});document.querySelector("[data-visitor-form]")?.addEventListener("submit",runReview);document.querySelector("[data-open-visitor-workflow]")?.addEventListener("click",()=>window.dispatchEvent(new CustomEvent("kairos:workflow-runtime:open",{detail:{workflowID:state.workflow?.id}})))}function headers(){return{"Content-Type":"application/json","X-MMG-Client-Build":BUILD}}async function request(url,init={}){const response=await fetch(url,{cache:"no-store",credentials:"include",...init});const text=await response.text();let body={};try{body=text?JSON.parse(text):{}}catch{body={message:text}}return{response,body}}function escapeHTML(value){return String(value??"").replace(/[&<>'"]/g,character=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[character])}