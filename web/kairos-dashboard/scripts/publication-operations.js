const BUILD="kairos-publication-operations-ui-20260713-1";
const ACTIVE_KEY="kairos.production.active-workspace";
let record=null,busy=false,error="";

function projectId(){try{const a=JSON.parse(sessionStorage.getItem(ACTIVE_KEY)||"null");return a?.workspace==="manuscript-studio"?a.projectId:null;}catch{return null;}}
async function enhance(){const anchor=document.querySelector("#publication-catalog");if(!anchor||document.querySelector("#publication-operations"))return;const id=projectId();if(!id)return;const section=document.createElement("section");section.id="publication-operations";section.className="publication-operations";anchor.insertAdjacentElement("afterend",section);await load(id);}
async function load(id){busy=true;render(id);try{const r=await fetch(`/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-operations`,{credentials:"include",cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Unable to load publication operations.");record=b.operations||null;error="";}catch(e){error=e.message||"Unable to load publication operations.";}finally{busy=false;render(id);}}
function render(id){const s=document.querySelector("#publication-operations");if(!s)return;if(busy){s.innerHTML='<p class="eyebrow">Post-publication operations</p><h3>Updating publication record…</h3>';return;}if(error){s.innerHTML=`<p class="eyebrow">Post-publication operations</p><h3>Operations need attention</h3><p class="manuscript-error">${esc(error)}</p><button class="secondary" data-retry>Retry</button>`;s.querySelector("[data-retry]")?.addEventListener("click",()=>load(id));return;}const r=record||{};const open=(r.events||[]).filter(x=>x.status==="open");s.innerHTML=`<p class="eyebrow">Post-publication operations</p><h3>${r.status==="needs-attention"?"Publication needs attention":"Publication operating record"}</h3><p>Record confirmed live checks, pricing changes, corrections, revised editions, and availability incidents. Sales and royalties are not inferred.</p><div class="publication-operations-proof"><span><strong>${esc(r.publicationStatus||"active")}</strong><small>status</small></span><span><strong>${esc(r.activeEdition||"—")}</strong><small>edition</small></span><span><strong>${Number(r.openIssues||0)}</strong><small>open issues</small></span></div><div class="manuscript-actions"><button class="primary" data-check>Verify Live Publication</button><a class="secondary ops-export" href="/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-operations/export" download>Export Record</a></div><div class="manuscript-grid"><label>Event type<select data-type><option value="metadata-change">Metadata change</option><option value="price-change">Price change</option><option value="availability-incident">Availability incident</option><option value="correction">Correction</option><option value="revised-edition">Revised edition</option><option value="relaunch">Relaunch</option><option value="retirement-review">Retirement review</option></select></label><label>Event title<input data-title maxlength="180"></label></div><div class="manuscript-grid"><label>Previous value<input data-old maxlength="1000"></label><label>New value<input data-new maxlength="1000"></label></div><label>Operations note<textarea data-note maxlength="4000"></textarea></label><button class="primary" data-add>Record Event</button>${open.length?`<div class="issue-list publication-operations-list">${open.map(x=>`<article><b>${esc(x.title)}</b><p>${esc(x.type)}</p><button class="secondary" data-resolve="${esc(x.eventId)}">Resolve</button></article>`).join("")}</div>`:""}`;s.querySelector("[data-check]")?.addEventListener("click",()=>run(id,"check-live",{actor:"Executive"}));s.querySelector("[data-add]")?.addEventListener("click",()=>add(id));s.querySelectorAll("[data-resolve]").forEach(btn=>btn.addEventListener("click",()=>resolve(id,btn.dataset.resolve)));}
async function add(id){const q=x=>document.querySelector(x);await run(id,"events",{type:q("[data-type]")?.value,title:q("[data-title]")?.value,note:q("[data-note]")?.value,previousValue:q("[data-old]")?.value,newValue:q("[data-new]")?.value,actor:"Executive"});}
async function resolve(id,eventId){const resolution=window.prompt("Resolution note")||"Resolved";await run(id,`resolve/${encodeURIComponent(eventId)}`,{resolution,actor:"Executive"});}
async function run(id,action,payload){busy=true;render(id);try{const r=await fetch(`/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-operations/${action}`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},body:JSON.stringify(payload)});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Publication operations action failed.");record=b.operations||null;error="";await window.KairosProductionWorkspace?.refresh?.();}catch(e){error=e.message||"Publication operations action failed.";}finally{busy=false;render(id);}}
function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);}
new MutationObserver(enhance).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener("kairos:production:state-changed",enhance);enhance();
