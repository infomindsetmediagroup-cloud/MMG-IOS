import{getOpsAlerts}from'./ops-alert-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-ops-alert-panel]'))return;const a=getOpsAlerts();const card=document.createElement('article');card.className='card full';card.dataset.opsAlertPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Alerts</p><h3>Operations Alerts</h3></div><span class="badge">${a.alerts.length}</span></div><div class="list" style="margin-top:16px;">${a.alerts.map(i=>`<div class="list-item"><div><strong>${e(i.title)}</strong><p class="muted">${e(i.detail)} • ${e(a.updated)}</p></div><span class="${i.level==='good'?'badge good':'badge warning'}">${e(i.level)}</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);