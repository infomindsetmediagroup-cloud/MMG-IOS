const BUILD='kairos-support-offer-learning-ui-20260715-1';new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{if(event.target.closest?.('[data-child="support-intelligence"],[data-child="offer-builder"]'))setTimeout(attach,100)});attach();function attach(){for(const selector of['#support-intelligence','.support-intelligence','#offer-builder','.offer-builder']){const host=document.querySelector(selector);if(!host||host.querySelector('[data-support-offer-learning]'))continue;const section=document.createElement('section');section.dataset.supportOfferLearning='';section.innerHTML=`<div class="governed-boundary"><strong>Support Intelligence → Offer Builder</strong><p>Convert verified support portfolio evidence into an approval-gated offer decision with successor lineage and a measured adoption receipt. No pricing, offer, claims, launch, communication, customer-facing, publication, or external execution occurs automatically.</p><button type="button" data-open-support-offer>Open Support Learning Review</button></div>`;host.appendChild(section);section.querySelector('button').addEventListener('click',open)}}async function open(){const supportPortfolioID=prompt('Closed Support Intelligence portfolio receipt ID');if(!supportPortfolioID)return;const offerID=prompt('Governed Offer Builder offer ID');const actor=prompt('Review actor');const evidence=prompt('Verified authoritative support evidence');const assessment=prompt('Bounded offer assessment');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const data=await api('/api/support-offer-learning',{method:'POST',body:{supportPortfolioID,offerID,actor,evidence,assessment,successMetric,stopCondition}});alert(`Support offer-learning record ${data.record.id} created. Complete and approve workflow ${data.record.workflowID}, then record the verified adoption receipt.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Support offer-learning operation failed.');return data}window.KairosSupportOfferLearning={build:BUILD,attach};