import{getExecutionLedger}from'./execution-ledger-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-execution-ledger-panel]'))return;const l=getExecutionLedger();const card=document.createElement('article');card.className='card full';card.dataset.executionLedgerPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Ledger</p><h3>Execution Ledger</h3></div><span class="${l.ready?'badge good':'badge warning'}">${l.ready?'Clean':'Review'}</span></div><p class="muted" style="margin-top:12px;">${e(l.summary)}</p><div class="list" style="margin-top:16px;"><div class="list-item"><div><strong>${e(l.entry)}</strong><p class="muted">Latest execution ledger entry</p></div><span class="badge">${e(l.status)}</span></div></div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);