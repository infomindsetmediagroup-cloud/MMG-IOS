import{getFinalHandoff}from'./final-handoff-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-final-handoff-panel]'))return;const h=getFinalHandoff();const card=document.createElement('article');card.className='card full';card.dataset.finalHandoffPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Handoff</p><h3>Final Handoff</h3></div><span class="${h.ready?'badge good':'badge warning'}">${h.ready?'Ready':'Needs Review'}</span></div><p class="muted" style="margin-top:12px;">${e(h.summary)} • ${e(h.updated)}</p><div class="list" style="margin-top:16px;"><div class="list-item"><div><strong>${e(h.headline)}</strong><p class="muted">Final execution handoff</p></div><span class="badge">${e(h.status)}</span></div><div class="list-item"><strong>Checkpoint</strong><span class="badge">${e(h.checkpoint)}</span></div></div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);