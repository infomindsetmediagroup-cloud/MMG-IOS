import{getReadinessGate}from'./readiness-gate-engine.js';export function renderReadinessGate(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-readiness-gate]'))return;const g=getReadinessGate();const c=document.createElement('article');c.className='card full';c.dataset.readinessGate='true';c.innerHTML=`<div class='card-header'><div><p class='eyebrow'>Readiness</p><h3>Launch Readiness Gate</h3></div><span class='badge'>${g.gate}</span></div><div class='list'>${g.checks.map(x=>`<div class='list-item'><strong>${x.name}</strong><span class='badge'>${x.pass?'PASS':'FAIL'}</span></div>`).join('')}</div>`;v.prepend(c)};window.addEventListener('DOMContentLoaded',renderReadinessGate);