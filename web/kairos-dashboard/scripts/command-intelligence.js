const rules=[['Shopify',['shopify','product','listing','price','bundle']],['Website',['website','page','image','hero','section']],['Content',['content','article','post','knowledge']],['Quality',['qa','test','bug','verify','release']],['Growth',['campaign','marketing','promo','email']]];export function analyzeCommand(t){const s=String(t||'').toLowerCase();let lane='Operations',hits=0;for(const [l,terms] of rules){const n=terms.filter(x=>s.includes(x)).length;if(n>hits){hits=n;lane=l}}const confidence=Math.min(99,45+hits*18+(s.length>24?10:0));const action=s.includes('fix')?'Fix':s.includes('create')||s.includes('add')?'Create':s.includes('update')?'Update':s.includes('verify')?'Verify':'Route';return{lane,confidence,action,summary:`${action} request routed to ${lane}.`}}