import{getSecurityAudit}from'./security-audit-engine.js';function e(v){return String(v||'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-security-audit-panel]'))return;const a=getSecurityAudit();const passed=a.checks.filter(c=>c.passed).length;const card=document.createElement('article');card.className='card full';card.dataset.securityAuditPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Audit</p><h3>Security Audit</h3></div><span class="badge warning">${e(a.status)}</span></div><p class="muted" style="margin-top:12px;">${passed}/${a.checks.length} checks currently cleared • ${e(a.updated)}</p><div class="list" style="margin-top:16px;">${a.checks.map(c=>`<div class="list-item"><strong>${e(c.name)}</strong><span class="${c.passed?'badge good':'badge warning'}">${c.passed?'Passed':'Open'}</span></div>`).join('')}</div>`;v.prepend(card)}window.addEventListener('DOMContentLoaded',render);window.addEventListener('kairos:auth',render);