import{saveOperatorCheckpoint,getOperatorCheckpoints}from'./operator-checkpoint-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-operator-checkpoint-panel]'))return;const items=getOperatorCheckpoints();const card=document.createElement('article');card.className='card full';card.dataset.operatorCheckpointPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Checkpoint</p><h3>Operator Checkpoints</h3></div><span class="badge">${items.length}</span></div><div class="action-row"><button class="action-button" data-save-checkpoint>Save Checkpoint</button></div><div class="list" style="margin-top:16px;">${(items.length?items:[{note:'No checkpoints saved yet',summary:'Pending first checkpoint',updated:'Pending',status:'Standby'}]).slice(0,6).map(i=>`<div class="list-item"><div><strong>${e(i.note)}</strong><p class="muted">${e(i.summary)} • ${e(i.updated)}</p></div><span class="badge">${e(i.status)}</span></div>`).join('')}</div>`;v.prepend(card);card.querySelector('[data-save-checkpoint]').addEventListener('click',()=>{saveOperatorCheckpoint();card.remove();render()})}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);