const BUILD="kairos-customer-journey-ui-20260713-1";const state={open:false,loading:false,error:"",journey:null,workflow:null};start();function start(){document.addEventListener("click",intercept,true);window.addEventListener("kairos:customer-journeys:open",openWorkspace)}function intercept(event){const button=event.target.closest?.('[data-child="customer-journeys"]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();openWorkspace()}async function openWorkspace(){state.open=true;await loadLatest();render();setTimeout(()=>document.querySelector("#customer-journeys")?.scrollIntoView({behavior:"smooth",block:"start"}),20)}async function loadLatest(){try{const{response,body}=await request("/api/customer-journeys/latest");if(response.ok){state.journey=body.journey||null;state.workflow=body.workflow||null}}catch{}}async function createJourney(event){event.preventDefault();const data=new FormData(event.currentTarget);state.loading=true;state.error="";render();try{const{response,body}=await request("/api/customer-journeys",{method:"POST",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},body:JSON.stringify(Object.fromEntries(data.entries()))});if(!response.ok)throw new Error(body?.error?.message||"Kairos could not build the customer journey.");state.journey=body.journey;state.workflow=body.workflow}catch(error){state.error=error.message||"Kairos could not build the customer journey."}finally{state.loading=false;render()}}function render(){const hub=document.querySelector("#kairos-hub");if(!hub)return;let root=document.querySelector("#customer-journeys");if(!state.open){root?.remove();return}if(!root){root=document.createElement("section");root.id="customer-journeys";root.className="customer-journeys workspace";hub.appendChild(root)}root.innerHTML=`<header class="journey-head"><div><p class="eyebrow">Customers · Customer Journeys</p><h2>Customer Journey Workspace</h2><p>Map the current experience, identify friction, and define the governed future-state journey.</p></div><button type="button" data-close-journey>Close</button></header>${state.error?`<p class="journey-error">${esc(state.error)}</p>`:""}<div class="journey-layout"><section class="journey-form"><form data-journey-form><label>Journey name<input name="title" required maxlength="180" placeholder="Example: First-time customer onboarding"></label><label>Finished experience objective<textarea name="objective" required maxlength="3000"></textarea></label><label>Customer segment<textarea name="customerSegment" maxlength="1800"></textarea></label><label>Entry point<input name="entryPoint" maxlength="1200"></label><label>Desired outcome<textarea name="desiredOutcome" maxlength="1800"></textarea></label><label>Current-state journey<textarea name="currentState" maxlength="4000"></textarea></label><label>Touchpoints and handoffs<textarea name="touchpoints" maxlength="5000"></textarea></label><label>Friction and failure points<textarea name="friction" maxlength="4000"></textarea></label><label>Future-state journey<textarea name="futureState" maxlength="5000"></textarea></label><label>Owners and responsibilities<textarea name="owners" maxlength="1800"></textarea></label><label>Service standards<textarea name="serviceStandards" maxlength="2400"></textarea></label><label>Success metrics<textarea name="successMetrics" maxlength="2400"></textarea></label><label>Constraints<textarea name="constraints" maxlength="2400"></textarea></label><label>Priority<select name="priority"><option value="high">High</option><option value="normal" selected>Normal</option><option value="critical">Critical</option><option value="low">Low</option></select></label><label class="journey-check"><input type="checkbox" name="approvalRequired" checked> Require approval before customer-facing changes</label><button class="primary" type="submit">Create Journey + Workflow</button></form></section><section class="journey-current">${state.loading?`<p class="journey-loading">Kairos is building the journey map…</p>`:journeyMarkup()}</section></div>`;bind()}function journeyMarkup(){if(!state.journey||!state.workflow)return`<div class="journey-empty"><strong>No journey is open.</strong><p>Create a journey and Kairos will place its five governed stages into the Work Queue.</p></div>`;const j=state.journey,w=state.workflow;return`<article class="journey-package"><p class="eyebrow">${esc(j.status)}</p><h3>${esc(j.title)}</h3><p>${esc(j.objective)}</p><div class="journey-stats"><div><strong>${Number(w.progress||0)}%</strong><span>Progress</span></div><div><strong>${w.tasks?.length||0}</strong><span>Stages</span></div><div><strong>${esc(w.approvalStatus||"not-required")}</strong><span>Approval</span></div></div><div class="journey-boundary"><strong>Customer governance</strong><p>Customer-facing changes and automated messaging require approval. Kairos does not perform automatic profiling or unsupported identity inference.</p></div><div class="journey-actions"><button data-open-journey-workflow>Open in Work Queue</button><button data-new-journey>Map Another Journey</button></div></article>`}function bind(){document.querySelector("[data-close-journey]")?.addEventListener("click",()=>{state.open=false;render()});document.querySelector("[data-journey-form]")?.addEventListener("submit",createJourney);document.querySelector("[data-new-journey]")?.addEventListener("click",()=>{state.journey=null;state.workflow=null;render()});document.querySelector("[data-open-journey-workflow]")?.addEventListener("click",()=>window.dispatchEvent(new CustomEvent("kairos:workflow-runtime:open",{detail:{workflowID:state.workflow?.id}})))}async function request(url,init={}){const response=await fetch(url,{cache:"no-store",credentials:"include",...init});const text=await response.text();let body={};try{body=text?JSON.parse(text):{}}catch{body={message:text}}return{response,body}}function esc(value){return String(value??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c])}