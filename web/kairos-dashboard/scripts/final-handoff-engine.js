import{getExecutionStatus}from'./execution-status-engine.js';import{getOperatorCheckpoints}from'./operator-checkpoint-engine.js';export function getFinalHandoff(){const s=getExecutionStatus();const checkpoints=getOperatorCheckpoints();return{status:s.state,headline:s.headline,summary:s.summary,checkpoint:checkpoints[0]?.note||'No checkpoint saved',ready:s.alerts===0,updated:new Date().toLocaleString()};}