const BUILD='kairos-campaign-offer-learning-ui-20260715-1';new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest?.('[data-child="campaign-operations"],[data-child="offer-builder"]'))setTimeout(attach,100)});attach();function attach(){for(const selector of['#campaign-operations','.campaign-operations','#offer-builder','.offer-builder']){const host=document.querySelector(selector);if(!host||host.querySelector('[data-campaign-offer-learning]'))continue;const section=document.createElement('section');section.dataset.campaignOfferLearning='';section.innerHTML=`<div class="governed-boundary"><strong>Campaign Operations → Offer Builder</strong><p>Adopt verified campaign portfolio evidence into a bounded offer decision. Revisions require successor-offer lineage. No pricing, offer, claims, launch, customer-facing, publication, or campaign-execution change occurs automatically.</p><button type="button" data-open-campaign-offer-learning>Open Campaign Learning Review</button></div>`;host.appendChild(section);section.querySelector('button').addEventListener('click',open)}}async function open(){const campaignPortfolioID=prompt('Closed Campaign Operations portfolio ID');if(!campaignPortfolioID)return;const offerMandateID=prompt('Certified governed offer mandate ID');const actor=prompt('Review actor');const evidence=prompt('Verified campaign evidence');const assessment=prompt('Offer-learning assessment');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const data=await api('/api/campaign-offer-learning',{method:'POST',body:{campaignPortfolioID,offerMandateID,actor,evidence,assessment,successMetric,stopCondition}});alert(`Campaign offer-learning review ${data.record.id} created. Complete and approve workflow ${data.record.workflowID}, then record the verified adoption receipt.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Campaign offer learning operation failed.');return data}window.KairosCampaignOfferLearning={build:BUILD,attach};