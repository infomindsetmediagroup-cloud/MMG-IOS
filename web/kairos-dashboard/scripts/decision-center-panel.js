import{getDecisionCenter}from'./decision-center-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-decision-center-panel]'))return;const d=getDecisionCenter();const card=document.createElement('article');card.className='card full';card.dataset.decisionCenterPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Decision</p><h3>Decision Center</h3></div><span class="${d.decision==='EXECUTE'?'badge good':'badge warning'}">${e(d.decision)}</span></div><p class="muted" style="margin-top:12px;">${e(d.reason)} • ${e(d.updated)}</p><div class="list" style="margin-top:16px;">${d.queue.map((i,n)=>`<div class="list-item"><strong>${n+1}. ${e(i)}</strong><span class="badge">${n?'Queued':'Primary'}</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);