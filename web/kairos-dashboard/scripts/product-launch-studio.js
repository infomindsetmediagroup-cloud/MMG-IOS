const BUILD="kairos-product-launch-studio-ui-20260714-3";const state={open:false,loading:false,error:"",project:null,workflow:null,prefill:null};start();function start(){document.addEventListener("click",intercept,true);window.addEventListener("kairos:product-launch:open",e=>openStudio(e.detail||{}))}function intercept(e){const b=e.target.closest?.('[data-child="product-launch"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();openStudio()}async function openStudio(detail={}){state.open=true;state.prefill=detail;await loadLatest();render();setTimeout(()=>document.querySelector("#product-launch-studio")?.scrollIntoView({behavior:"smooth",block:"start"}),20)}async function loadLatest(){try{const{response,body}=await request("/api/product-launch/latest");if(response.ok){state.project=body.project||null;state.workflow=body.workflow||null}}catch{}}async function createProject(e){e.preventDefault();const d=new FormData(e.currentTarget);state.loading=true;state.error="";render();try{const{response,body}=await request("/api/product-launch/projects",{method:"POST",headers:headers(),body:JSON.stringify({title:d.get("title"),objective:d.get("objective"),productType:d.get("productType"),audience:d.get("audience"),problem:d.get("problem"),promise:d.get("promise"),positioning:d.get("positioning"),price:d.get("price"),delivery:d.get("delivery"),channels:d.get("channels"),launchDate:d.get("launchDate"),successMetric:d.get("successMetric"),dependencies:d.get("dependencies"),priority:d.get("priority"),approvalRequired:d.get("approvalRequired")==="on"})});if(!response.ok)throw new Error(body?.error?.message||"Kairos could not create the launch project.");state.project=body.project;state.workflow=body.workflow}catch(x){state.error=x.message}finally{state.loading=false;render()}}function nextAction(){const w=state.workflow;if(!w)return"Create the launch plan";if(w.approvalRequired&&w.approvalStatus!=="approved")return"Approve pricing, positioning, and release scope";if(w.state==="blocked")return"Resolve the blocked launch dependency";if(Number(w.progress||0)>=100&&w.state!=="completed")return"Finish and preserve the launch receipt";if(w.state==="active")return"Continue the next launch stage";return"Open the launch work"}function render(){const hub=document.querySelector("#kairos-hub");if(!hub)return;let root=document.querySelector("#product-launch-studio");if(!state.open){root?.remove();return}if(!root){root=document.createElement("section");root.id="product-launch-studio";root.className="product-launch-studio workspace";hub.appendChild(root)}const p=state.prefill||{};root.innerHTML=`<header class="launch-studio-head"><div><p class="eyebrow">Business · Product Launch</p><h2>Product Launch Operations</h2><p>Turn an approved offer into a coordinated launch package, release decision, and measurable rollout.</p></div><button data-close-launch-studio>Close</button></header>${state.error?`<p class="launch-studio-error">${esc(state.error)}</p>`:""}<div class="launch-studio-layout"><section class="launch-studio-form"><h3>New Launch</h3><form data-launch-studio-form><label>Product or offer name<input name="title" required maxlength="180" value="${esc(p.title||"")}"></label><label>Finished launch objective<textarea name="objective" required>${esc(p.objective||"")}</textarea></label><div class="launch-studio-fields"><label>Product type<select name="productType"><option value="digital-download">Digital download</option><option value="book">Book</option><option value="physical-product">Physical product</option><option value="merchandise">Merchandise</option><option value="service">Service</option><option value="subscription">Subscription</option><option value="bundle">Bundle</option><option value="custom">Custom</option></select></label><label>Priority<select name="priority"><option value="critical">Critical</option><option value="high" selected>High</option><option value="normal">Normal</option><option value="low">Low</option></select></label></div><label>Target customer<input name="audience" value="${esc(p.audience||"")}"></label><label>Problem addressed<textarea name="problem">${esc(p.problem||"")}</textarea></label><label>Customer promise<textarea name="promise">${esc(p.promise||"")}</textarea></label><label>Positioning<textarea name="positioning">${esc(p.positioning||"")}</textarea></label><div class="launch-studio-fields"><label>Price or pricing model<input name="price" value="${esc(p.price||"")}"></label><label>Launch date<input name="launchDate"></label></div><label>Delivery and fulfillment<textarea name="delivery">${esc(p.delivery||"")}</textarea></label><label>Launch channels<textarea name="channels"></textarea></label><label>Primary success metric<input name="successMetric"></label><label>Dependencies and constraints<textarea name="dependencies">${esc(p.dependencies||"")}${p.offerReference?`\nOffer reference: ${esc(p.offerReference)}`:""}</textarea></label><label class="launch-studio-check"><input type="checkbox" name="approvalRequired" checked> Require approval before release work starts</label><button class="primary" type="submit">Build Launch Plan</button></form></section><section class="launch-studio-current">${state.loading?`<p class="launch-studio-loading">Kairos is building the launch plan…</p>`:currentProject()}</section></div>`;bind()}function currentProject(){if(!state.project||!state.workflow)return`<div class="launch-studio-empty"><strong>No launch is open.</strong><p>Build one launch and Kairos will coordinate its work inside My Work.</p></div>`;const p=state.project,w=state.workflow;return`<div class="launch-studio-project"><p class="eyebrow">${esc(p.productType?.replaceAll("-"," ")||"Launch")}</p><h3>${esc(p.title)}</h3><p>${esc(p.objective)}</p><div class="launch-studio-focus"><span>Next best action</span><strong>${esc(nextAction())}</strong></div><div class="launch-studio-stats"><div><strong>${Number(w.progress||0)}%</strong><span>Progress</span></div><div><strong>${w.tasks?.length||0}</strong><span>Stages</span></div><div><strong>${esc(w.state||"ready")}</strong><span>Status</span></div></div><div class="launch-studio-boundary"><strong>Release boundary</strong><p>Pricing, public publication, campaign activation, and irreversible release actions remain approval-gated.</p></div><div class="launch-studio-actions"><button data-open-my-work>Open My Work</button><button data-open-offer-builder>Refine Offer</button><button data-open-launch-release>Authorize Storefront Release</button><button data-new-launch-project>Start Another Launch</button></div></div>`}function bind(){document.querySelector("[data-close-launch-studio]")?.addEventListener("click",()=>{state.open=false;render()});document.querySelector("[data-launch-studio-form]")?.addEventListener("submit",createProject);document.querySelector("[data-new-launch-project]")?.addEventListener("click",()=>{state.project=null;state.workflow=null;state.prefill=null;render()});document.querySelector("[data-open-my-work]")?.addEventListener("click",()=>window.KairosExecutiveSimpleMode?.openMyWork?.());document.querySelector("[data-open-offer-builder]")?.addEventListener("click",()=>{const p=state.project||{};window.dispatchEvent(new CustomEvent("kairos:offer-builder:open",{detail:{title:p.title,audience:p.audience,problem:p.problem,promise:p.promise,positioning:p.positioning,price:p.price,delivery:p.delivery,objective:p.objective,dependencies:p.dependencies}}))})}function headers(){return{"Content-Type":"application/json","X-MMG-Client-Build":BUILD}}async function request(url,init={}){const response=await fetch(url,{cache:"no-store",credentials:"include",...init});const text=await response.text();let body={};try{body=text?JSON.parse(text):{}}catch{body={message:text}}return{response,body}}function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c])}import('./product-launch-release-loop.js');