const BUILD="kairos-homepage-quick-action-20260716-1";
const ROUTE="/center/content/website";
const STATE_KEY="kairos.homepage.quick-action.v1";
const state=loadState();

mount();
window.addEventListener("popstate",()=>requestAnimationFrame(mount));
document.addEventListener("click",event=>{if(event.target?.closest?.('[data-route-workspace="website"], [data-route-center="content"], [data-route-home]'))requestAnimationFrame(mount);});

function mount(){if(location.pathname!==ROUTE)return;const host=document.querySelector(".child-workspace-page .job.routed-job");if(!host||host.dataset.homepageQuickAction===BUILD)return;host.dataset.homepageQuickAction=BUILD;render(host);}
function render(host=document.querySelector(".child-workspace-page .job.routed-job")){if(!host||location.pathname!==ROUTE)return;host.dataset.homepageQuickAction=BUILD;if(["planning","executing","verifying","publishing"].includes(state.mode))host.innerHTML=workingMarkup();else if(state.mode==="preview")host.innerHTML=previewMarkup();else if(state.mode==="release")host.innerHTML=releaseMarkup();else if(state.mode==="complete")host.innerHTML=completionMarkup();else host.innerHTML=inputMarkup();bind(host);}

function inputMarkup(){return `<section class="homepage-quick-action"><div class="homepage-quick-hero"><p class="eyebrow">Homepage Quick Action</p><h2>Tell Kairos what to change.</h2><p>Kairos reads the current Shopify staging homepage, rewrites only the necessary customer-facing copy, preserves the existing design, builds the staging result, and returns the preview.</p></div><div class="homepage-design-lock"><strong>Current design locked</strong><span>Colors · typography · pills · cards · spacing · section order · layout · links · template · stylesheet</span></div><label class="objective-label" for="homepage-quick-objective">What should the homepage say or accomplish?</label><textarea class="objective" id="homepage-quick-objective" maxlength="12000" placeholder="Example: Explain that MMG helps creators turn their knowledge into professional digital products, while keeping the current design exactly as it is.">${escapeHTML(state.objective)}</textarea>${state.error?`<p class="homepage-quick-error">${escapeHTML(state.error)}</p>`:""}<button class="primary homepage-quick-build" type="button" data-homepage-quick-build>Build My Homepage Preview</button><small class="homepage-quick-boundary">Kairos writes only to the verified non-live staging theme. Publishing the preview still requires your explicit approval.</small></section>`;}
function workingMarkup(){const copy={planning:["Reading the current homepage","Kairos is inspecting the exact staging source and binding your objective to the existing visible copy."],executing:["Building the preserved-design homepage","Kairos is writing only the approved copy changes and proving that the template, stylesheet, markup, and styled text nodes remain unchanged."],verifying:["Preparing the real Shopify preview","Kairos is probing the rendered staging homepage for mobile and desktop review."],publishing:["Applying and verifying the approved homepage","Kairos is saving the exact approved preview and preserving rollback evidence."]}[state.mode]||["Working","Kairos is completing the homepage job."];return `<section class="homepage-quick-action homepage-quick-working"><div class="homepage-quick-spinner" aria-hidden="true"></div><p class="eyebrow">Kairos is working</p><h2>${escapeHTML(copy[0])}</h2><p>${escapeHTML(copy[1])}</p><div class="homepage-quick-progress"><span></span></div></section>`;}
function previewMarkup(){const preview=state.verification?.preview||{},checks=Array.isArray(state.verification?.automatedChecks)?state.verification.automatedChecks:[],passed=checks.filter(check=>check?.passed).length;return `<section class="homepage-quick-action"><div class="homepage-quick-success"><p class="eyebrow">Homepage preview ready</p><h2>Your current design is preserved.</h2><p>${escapeHTML(state.execution?.summary||"Kairos updated the staging homepage copy and verified the preserved-design contract.")}</p></div><div class="homepage-preview-links"><a href="${escapeAttribute(preview.mobileURL||preview.url||"#")}" target="_blank" rel="noopener">Open Mobile Preview ↗</a><a href="${escapeAttribute(preview.desktopURL||preview.url||"#")}" target="_blank" rel="noopener">Open Desktop Preview ↗</a></div><div class="homepage-preview-evidence"><strong>${passed}/${checks.length||passed} rendered checks passed</strong><span>Template unchanged · stylesheet unchanged · markup preserved · staging only</span></div>${state.error?`<p class="homepage-quick-error">${escapeHTML(state.error)}</p>`:""}<label class="homepage-quick-confirm"><input type="checkbox" data-homepage-preview-reviewed><span>I reviewed this exact staging preview and want Kairos to prepare it for live application.</span></label><div class="homepage-quick-actions"><button class="primary" type="button" data-homepage-preview-approve>Approve This Preview</button><button type="button" data-homepage-start-over>Change the Request</button></div></section>`;}
function releaseMarkup(){const files=Array.isArray(state.release?.files)?state.release.files:[];return `<section class="homepage-quick-action"><p class="eyebrow">Final approval</p><h2>Apply the approved homepage?</h2><p>Kairos will apply only the verified staging files below, save them in Shopify, read them back, and preserve rollback evidence.</p><div class="homepage-preview-evidence"><strong>${escapeHTML(files.map(file=>file.filename).join(", ")||"Verified homepage package")}</strong><span>Live application requires this final authorization.</span></div>${state.error?`<p class="homepage-quick-error">${escapeHTML(state.error)}</p>`:""}<label class="homepage-quick-confirm danger"><input type="checkbox" data-homepage-live-approved><span>I authorize Kairos to apply, save, and verify this exact approved homepage on the live Shopify theme.</span></label><div class="homepage-quick-actions"><button class="primary" type="button" data-homepage-publish>Apply & Save Homepage</button><button type="button" data-homepage-back-preview>Back to Preview</button></div></section>`;}
function completionMarkup(){const release=state.release||{},probe=release.publication?.liveProbe||release.rollback?.liveProbe||{},rolledBack=String(release.status||"").startsWith("rolled-back");return `<section class="homepage-quick-action"><p class="eyebrow">${rolledBack?"Rollback verified":"Homepage completed"}</p><h2>${rolledBack?"The previous homepage was restored.":"The homepage is live and verified."}</h2><p>HTTP ${escapeHTML(probe.status??"—")} · ${escapeHTML(probe.title||"Shopify storefront verification recorded")}</p>${probe.finalURL?`<a class="homepage-live-link" href="${escapeAttribute(probe.finalURL)}" target="_blank" rel="noopener">Open Live Website ↗</a>`:""}${state.error?`<p class="homepage-quick-error">${escapeHTML(state.error)}</p>`:""}<div class="homepage-quick-actions"><button class="primary" type="button" data-homepage-start-over>Start Another Homepage Job</button>${!rolledBack&&release.publication?`<button class="danger" type="button" data-homepage-rollback>Emergency Rollback</button>`:""}</div></section>`;}

function bind(host){host.querySelector("[data-homepage-quick-build]")?.addEventListener("click",buildPreview);host.querySelector("[data-homepage-preview-approve]")?.addEventListener("click",approvePreview);host.querySelector("[data-homepage-publish]")?.addEventListener("click",publishRelease);host.querySelectorAll("[data-homepage-start-over]").forEach(button=>button.addEventListener("click",startOver));host.querySelector("[data-homepage-back-preview]")?.addEventListener("click",()=>{state.mode="preview";state.error="";saveState();render();});host.querySelector("[data-homepage-rollback]")?.addEventListener("click",rollbackRelease);}

async function buildPreview(){const objective=document.querySelector("#homepage-quick-objective")?.value.trim()||"";if(objective.length<3){state.error="Tell Kairos what you want the homepage to say or accomplish.";render();return;}state.objective=objective;state.mode="planning";state.error="";state.execution=null;state.verification=null;state.release=null;saveState();render();try{const submittedPlan=await submitJob("/api/shopify/staging/plan/jobs",{objective,requestType:"homepage-preserve-design",intent:"homepage-preserve-design",homepageMode:"preserve-current-design",preserveExistingDesign:true,fullRetoolConfirmed:false,structuralMutationAuthorized:false,styleMutationAuthorized:false,contentOnlyLocked:false});const plan=await pollJob(submittedPlan,"plan");state.mode="executing";render();const approval={status:"approved",approvedAt:new Date().toISOString(),build:BUILD,planID:plan.planID,actionID:plan.actionID,targetThemeID:plan?.plan?.targetTheme?.gid||"",sourceHashes:plan?.plan?.sourceHashes||{},objective,scope:"verified-staging-preserve-design"};const submittedExecution=await submitJob("/api/shopify/staging/execute/jobs",{plan,approval});state.execution=await pollJob(submittedExecution,"execute");state.mode="verifying";render();state.verification=await requestJSON("/api/shopify/staging/visual-verification",{execution:state.execution.execution,result:state.execution,requestType:"homepage",path:"/"});state.mode="preview";saveState();render();}catch(error){state.mode="input";state.error=error?.message||"Kairos could not build the homepage preview.";saveState();render();}}
async function approvePreview(){if(!document.querySelector("[data-homepage-preview-reviewed]")?.checked){state.error="Confirm that you reviewed the staging preview before continuing.";render();return;}const reviewID=state.verification?.reviewID;if(!reviewID){state.error="The rendered preview receipt is missing. Build the preview again.";render();return;}try{state.error="";state.verification=await requestJSON("/api/shopify/staging/visual-approval",{reviewID,decision:"approved",actor:"Executive",notes:"Preserve-design homepage preview reviewed and approved."});state.release=await requestJSON("/api/shopify/homepage-release/prepare",{reviewID});state.mode="release";saveState();render();}catch(error){state.error=error?.message||"Kairos could not prepare the approved live release.";render();}}
async function publishRelease(){if(!document.querySelector("[data-homepage-live-approved]")?.checked){state.error="Confirm the final live application authorization.";render();return;}if(!state.release?.releaseID){state.error="The approved release package is missing.";render();return;}state.mode="publishing";state.error="";render();try{state.release=await requestJSON("/api/shopify/homepage-release/publish",{releaseID:state.release.releaseID,confirmation:"APPLY APPROVED HOMEPAGE",actor:"Executive"});state.mode="complete";saveState();render();}catch(error){state.mode="release";state.error=error?.message||"Kairos could not apply and verify the homepage.";saveState();render();}}
async function rollbackRelease(){if(!state.release?.releaseID)return;state.mode="publishing";state.error="";render();try{state.release=await requestJSON("/api/shopify/homepage-release/rollback",{releaseID:state.release.releaseID,confirmation:"ROLL BACK APPROVED HOMEPAGE",actor:"Executive"});state.mode="complete";}catch(error){state.mode="complete";state.error=error?.message||"Kairos could not verify the homepage rollback.";}saveState();render();}
function startOver(){Object.assign(state,freshState());sessionStorage.removeItem(STATE_KEY);render();}

async function submitJob(url,payload){const{response,body}=await fetchJSON(url,{method:"POST",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},credentials:"include",body:JSON.stringify(payload)});if(!response.ok||!body?.jobID)throw new Error(body?.error?.message||body?.summary||`Kairos returned ${response.status}.`);return body;}
async function pollJob(submitted,type){if(submitted?.status==="completed"&&submitted?.result)return submitted.result;const deadline=Date.now()+600000;while(Date.now()<deadline){await new Promise(resolve=>setTimeout(resolve,800));const{response,body}=await fetchJSON(submitted.pollURL||`/api/shopify/staging/${type}/jobs/${submitted.jobID}`,{credentials:"include"});if(body?.status==="completed"&&body?.result)return body.result;if(["needs-attention","failed","cancelled","blocked"].includes(body?.status)||(!response.ok&&response.status!==202))throw new Error(body?.error?.message||body?.summary||`${type} did not complete.`);}throw new Error("Kairos preserved the homepage job but it did not finish within ten minutes. Run it again.");}
async function requestJSON(url,payload){const{response,body}=await fetchJSON(url,{method:"POST",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},credentials:"include",body:JSON.stringify(payload)});if(!response.ok)throw new Error(body?.error?.message||body?.summary||`Kairos returned ${response.status}.`);return body;}
async function fetchJSON(url,init={}){const response=await fetch(url,{cache:"no-store",...init}),text=await response.text();let body={};try{body=text?JSON.parse(text):{};}catch{body={summary:text};}return{response,body};}
function freshState(){return{mode:"input",objective:"",execution:null,verification:null,release:null,error:""};}
function loadState(){try{const value=JSON.parse(sessionStorage.getItem(STATE_KEY)||"null");if(!value||typeof value!=="object")return freshState();const restored={...freshState(),...value};if(["planning","executing","verifying","publishing"].includes(restored.mode))restored.mode="input";return restored;}catch{return freshState();}}
function saveState(){try{sessionStorage.setItem(STATE_KEY,JSON.stringify({mode:state.mode,objective:state.objective,verification:state.verification?{reviewID:state.verification.reviewID,preview:state.verification.preview,automatedChecks:state.verification.automatedChecks}:null,release:state.release,execution:state.execution?{summary:state.execution.summary,status:state.execution.status,execution:state.execution.execution}:null,error:state.error}));}catch{}}
function escapeAttribute(value){return escapeHTML(value).replace(/`/g,"&#96;");}
function escapeHTML(value){return String(value??"").replace(/[&<>'"]/g,character=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[character]);}
window.KairosHomepageQuickAction={build:BUILD,mount,reset:startOver};
