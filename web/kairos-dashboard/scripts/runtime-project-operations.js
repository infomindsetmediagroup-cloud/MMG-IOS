const API="/api/kairos/runtime";
const root=document.querySelector("#kairos-hub");
const request=async(path,options={})=>{const response=await fetch(`${API}${path}`,{credentials:"same-origin",headers:{"Content-Type":"application/json",...(options.headers||{})},...options});if(!response.ok)throw new Error(`Request failed: ${response.status}`);return response.json();};
const escapeHtml=(value)=>String(value??"").replace(/[&<>"']/g,(char)=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[char]));
const states=["initialized","intake","objective_analysis","planning","awaiting_approval","queued","executing","quality_review","packaging","delivery","follow_up","archived","blocked","failed","cancelled"];
function render(payload){const projects=Array.isArray(payload?.projects)?payload.projects:[];const counts=Object.fromEntries(states.map((state)=>[state,projects.filter((project)=>project.state===state).length]));const active=projects.filter((project)=>!["archived","cancelled"].includes(project.state));root.insertAdjacentHTML("beforeend",`<section class="runtime-projects-panel" aria-labelledby="runtime-projects-title"><header><p class="eyebrow">Kairos runtime</p><h2 id="runtime-projects-title">Publishing project operations</h2><p>Live project state, approvals, queue position, quality review, packaging, and customer-visible progress.</p></header><div class="runtime-scorecard"><article><strong>${active.length}</strong><span>Active projects</span></article><article><strong>${counts.awaiting_approval||0}</strong><span>Awaiting approval</span></article><article><strong>${counts.queued||0}</strong><span>Queued</span></article><article><strong>${counts.blocked||0}</strong><span>Blocked</span></article><article><strong>${counts.quality_review||0}</strong><span>Quality review</span></article><article><strong>${counts.delivery||0}</strong><span>Delivery</span></article></div><div class="runtime-project-list">${projects.length?projects.map(projectCard).join(""):"<article><h3>No runtime projects</h3><p>Create a project through the authenticated runtime API.</p></article>"}</div></section>`);bind();}
function projectCard(project){const progress=project.progress||{};const approvals=project.approvals||[];const required=approvals.filter((item)=>item.required);const approved=required.filter((item)=>item.status==="approved").length;const deliverables=project.deliverables||[];return `<article class="runtime-project-card" data-project-id="${escapeHtml(project.projectId)}"><header><div><p class="runtime-state">${escapeHtml(project.state)}</p><h3>${escapeHtml(project.title)}</h3></div><strong>${Number(progress.percent||0)}%</strong></header><div class="runtime-progress" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${Number(progress.percent||0)}"><span style="width:${Math.max(0,Math.min(100,Number(progress.percent||0)))}%"></span></div><dl><div><dt>Stage</dt><dd>${escapeHtml(progress.stage||project.state)}</dd></div><div><dt>Approvals</dt><dd>${approved}/${required.length}</dd></div><div><dt>Queue</dt><dd>${escapeHtml(project.queue?.status||"not_queued")}</dd></div><div><dt>Deliverables</dt><dd>${deliverables.length}</dd></div></dl>${project.blockedReason?`<p class="runtime-blocked">${escapeHtml(project.blockedReason)}</p>`:""}<div class="runtime-actions"><button data-action="analyze">Analyze objective</button><button data-action="queue">Queue approved work</button><button data-action="start">Start execution</button><button data-action="qa">Record QA pass</button><button data-action="package">Record package</button><button data-action="deliver">Record delivery</button></div></article>`;}
function bind(){document.querySelectorAll(".runtime-project-card").forEach((card)=>{const id=card.dataset.projectId;card.querySelector('[data-action="analyze"]')?.addEventListener("click",()=>post(id,"analyze",{}));card.querySelector('[data-action="queue"]')?.addEventListener("click",()=>post(id,"queue",{priority:"normal"}));card.querySelector('[data-action="start"]')?.addEventListener("click",()=>post(id,"start",{}));card.querySelector('[data-action="qa"]')?.addEventListener("click",()=>event(id,{state:"packaging",event:{type:"qa_passed",state:"packaging",summary:"Quality review passed."},progress:{percent:80,stage:"packaging"}}));card.querySelector('[data-action="package"]')?.addEventListener("click",()=>event(id,{state:"delivery",event:{type:"package_created",state:"delivery",summary:"Deliverable package created."},progress:{percent:90,stage:"delivery"}}));card.querySelector('[data-action="deliver"]')?.addEventListener("click",()=>event(id,{state:"follow_up",event:{type:"delivered",state:"follow_up",summary:"Deliverables released to the customer."},progress:{percent:100,stage:"follow_up"}}));});}
async function post(id,action,input){await request(`/projects/${encodeURIComponent(id)}/${action}`,{method:"POST",body:JSON.stringify(input)});location.reload();}
async function event(id,input){await request(`/projects/${encodeURIComponent(id)}/events`,{method:"POST",body:JSON.stringify(input)});location.reload();}
(async()=>{try{render(await request("/projects"));}catch(error){root.insertAdjacentHTML("beforeend",`<section class="runtime-projects-panel"><h2>Publishing project operations</h2><p>${escapeHtml(error.message)}</p></section>`);}})();