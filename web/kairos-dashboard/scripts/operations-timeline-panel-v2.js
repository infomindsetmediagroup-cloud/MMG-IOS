import{getTimeline,addTimelineEvent}from'./operations-timeline-v2.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-ops-timeline-v2]'))return;const items=getTimeline();const card=document.createElement('article');card.className='card full';card.dataset.opsTimelineV2='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Timeline</p><h3>Operations Timeline v2</h3></div><span class="badge">${items.length}</span></div><div class="action-row"><button class="action-button" data-log-now>Log Checkpoint</button></div><div class="list" style="margin-top:16px;">${(items.length?items:[{title:'No timeline events yet',detail:'Create a checkpoint to start the stream.',time:'Pending',status:'Standby'}]).slice(0,12).map(i=>`<div class="list-item"><div><strong>${e(i.title||i.type||'Event')}</strong><p class="muted">${e(i.detail||i.status||'Logged')} • ${e(i.time)}</p></div><span class="badge">${e(i.status||'Logged')}</span></div>`).join('')}</div>`;v.prepend(card);card.querySelector('[data-log-now]').addEventListener('click',()=>{addTimelineEvent({title:'Operator checkpoint',detail:'Timeline checkpoint recorded.',status:'Logged'});card.remove();render();});}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render();});window.addEventListener('kairos:timeline-updated',()=>{document.querySelector('[data-ops-timeline-v2]')?.remove();render();});