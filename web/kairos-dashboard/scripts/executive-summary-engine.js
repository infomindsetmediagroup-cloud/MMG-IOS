import{getSystemHealthSummary}from'./system-health-summary.js';import{getOperatorRunbook}from'./operator-runbook.js';export function getExecutiveSummary(){const h=getSystemHealthSummary();const r=getOperatorRunbook();return{headline:`${h.status} - ${h.score}%`,summary:`${r.steps.length} operational steps queued for execution.`,updated:new Date().toLocaleString(),priorities:r.steps.slice(0,3)}}