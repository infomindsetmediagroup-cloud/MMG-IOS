const BUILD="kairos-revenue-engine-operations-20260727-1";const state={open:false,loading:false,error:"",products:[]};start();
function start(){window.addEventListener("kairos:revenue-engine:open",open);document.addEventListener("click",e=>{const b=e.target.closest?.("[data-open-revenue-engine]");if(!b)return;e.preventDefault();open()})}
async function open(){state.open=true;await refresh();render()}
async function refresh(){state.loading=true;state.error="";try{const r=await fetch("/api/kairos/revenue/products",{credentials:"include",cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Unable to load revenue products.");state.products=b.products||[]}catch(e){state.error=e.message}finally{state.loading=false}}
function render(){const hub=document.querySelector("#kairos-hub");if(!hub||!state.open)return;let root=document.querySelector("#revenue-engine-operations");if(!root){root=document.createElement("section");root.id="revenue-engine-operations";root.className="revenue-engine-operations workspace";hub.appendChild(root)}root.innerHTML=`<header><div><p class="eyebrow">Revenue · Shopify Asset Factory</p><h2>Revenue Engine</h2><p>Blueprint, manufacture, validate, approve, and hand off sellable Shopify products.</p></div><button data-close-revenue>Close</button></header>${state.error?`<p class="revenue-error">${esc(state.error)}</p>`:""}<div class="revenue-scorecards"><article><strong>${state.products.length}</strong><span>Products</span></article><article><strong>${count("ready_to_publish")}</strong><span>Ready</span></article><article><strong>${count("asset_generation")}</strong><span>Generating</span></article><article><strong>${count("quality_assurance")}</strong><span>QA</span></article></div><div class="revenue-products">${state.loading?"<p>Loading…</p>":state.products.map(card).join("")||"<p>No revenue products yet.</p>"}</div>`;bind()}
function card(p){const jobs=p.productionJobs||[],completed=jobs.filter(j=>j.state==="completed").length;return `<article class="revenue-product"><div><p class="eyebrow">${esc(p.blueprint?.productType||"product")}</p><h3>${esc(p.blueprint?.title||p.revenueProductId)}</h3><p>${esc(p.blueprint?.audience||"")}</p></div><dl><div><dt>State</dt><dd>${esc(p.state)}</dd></div><div><dt>Assets</dt><dd>${(p.assets||[]).length}/${(p.blueprint?.requiredAssets||[]).length}</dd></div><div><dt>Jobs</dt><dd>${completed}/${jobs.length}</dd></div><div><dt>QA</dt><dd>${esc(p.qualityAssurance?.status||"pending")}</dd></div></dl><div class="revenue-actions"><button data-revenue-action="plan-jobs" data-id="${esc(p.revenueProductId)}">Plan Jobs</button><button data-revenue-action="approve" data-id="${esc(p.revenueProductId)}">Approve</button><button data-revenue-action="shopify-handoff" data-id="${esc(p.revenueProductId)}">Shopify Handoff</button></div></article>`}
function bind(){document.querySelector("[data-close-revenue]")?.addEventListener("click",()=>{state.open=false;document.querySelector("#revenue-engine-operations")?.remove()});document.querySelectorAll("[data-revenue-action]").forEach(b=>b.addEventListener("click",()=>action(b.dataset.id,b.dataset.revenueAction)))}
async function action(id,name){state.loading=true;render();try{const r=await fetch(`/api/kairos/revenue/products/${encodeURIComponent(id)}/${name}`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},body:JSON.stringify({rationale:"Operator authorized revenue workflow action."})});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Revenue action failed.");await refresh()}catch(e){state.error=e.message;state.loading=false}render()}
function count(value){return state.products.filter(p=>p.state===value).length}function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c])}
