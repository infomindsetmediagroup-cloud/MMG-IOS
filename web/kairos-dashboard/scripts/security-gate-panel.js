import{getSecurityGate}from'./security-gate-engine.js';function e(v){return String(v||'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-security-gate-panel]'))return;const g=getSecurityGate();const card=document.createElement('article');card.className='card full';card.dataset.securityGatePanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">P1 Gate</p><h3>Security Gate</h3></div><span class="badge danger">${e(g.status)}</span></div><p class="muted" style="margin-top:12px;">${e(g.summary)} • ${e(g.updated)}</p><div class="list" style="margin-top:16px;">${g.checks.map(c=>`<div class="list-item"><strong>${e(c.name)}</strong><span class="${c.passed?'badge good':'badge warning'}">${c.passed?'Passed':'Required'}</span></div>`).join('')}</div>`;v.prepend(card)}window.addEventListener('DOMContentLoaded',render);window.addEventListener('kairos:auth',render);