import{getReadinessGate}from'./readiness-gate-engine.js';import{getLaunchControl}from'./launch-control-engine.js';export function getOperationsSnapshot(){const gate=getReadinessGate();const launch=getLaunchControl();return{timestamp:new Date().toLocaleString(),gate:gate.gate,launch:launch.status,score:gate.score,summary:`${gate.gate} • Launch ${launch.status} • Health ${gate.score}%`};}