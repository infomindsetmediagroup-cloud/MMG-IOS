import{getRBACStatus}from'./rbac-engine.js';function e(v){return String(v||'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-rbac-panel]'))return;const r=getRBACStatus();const card=document.createElement('article');card.className='card full';card.dataset.rbacPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Access</p><h3>Role-Based Access Control</h3></div><span class="badge good">${e(r.status)}</span></div><p class="muted" style="margin-top:12px;">${e(r.principle)} • ${e(r.enforcement)} • ${e(r.updated)}</p><div class="list" style="margin-top:16px;">${r.roles.map(role=>`<div class="list-item"><strong>${e(role)}</strong><span class="badge">Role</span></div>`).join('')}</div>`;v.prepend(card)}window.addEventListener('DOMContentLoaded',render);window.addEventListener('kairos:auth',render);