const BUILD='kairos-growth-offer-strategy-ui-20260715-1';new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{if(event.target.closest?.('[data-child="growth-plan"],[data-child="offer-builder"]'))setTimeout(attach,100)});attach();function attach(){for(const selector of['#growth-plan','.growth-plan','#offer-builder','.offer-builder']){const host=document.querySelector(selector);if(!host||host.querySelector('[data-growth-offer-strategy]'))continue;const section=document.createElement('section');section.dataset.growthOfferStrategy='';section.innerHTML=`<div class="governed-boundary"><strong>Growth Plan → Offer Builder</strong><p>Convert approved Growth Plan direction into a bounded offer strategy, certify measured offer outcomes, and return verified learning to the parent plan. No pricing, offer mutation, claims, customer-facing, product-launch, or publication change occurs automatically.</p><button type="button" data-open-growth-offer>Open Offer Strategy Handoff</button></div>`;host.appendChild(section);section.querySelector('button').addEventListener('click',open)}}async function open(){const growthPlanID=prompt('Growth Plan ID');if(!growthPlanID)return;const portfolioDecisionID=prompt('Approved Growth Plan portfolio decision ID');const offerID=prompt('Governed Offer Builder offer ID');const actor=prompt('Review actor');const evidence=prompt('Verified strategy evidence');const objective=prompt('Bounded offer objective');const customer=prompt('Intended customer');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const data=await api('/api/growth-offer-mandates',{method:'POST',body:{growthPlanID,portfolioDecisionID,offerID,actor,evidence,objective,customer,successMetric,stopCondition}});alert(`Offer strategy mandate ${data.record.id} created. Complete and approve workflow ${data.record.workflowID}, then certify the measured outcome and Growth Plan adoption receipt.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Growth offer strategy operation failed.');return data}window.KairosGrowthOfferStrategy={build:BUILD,attach};