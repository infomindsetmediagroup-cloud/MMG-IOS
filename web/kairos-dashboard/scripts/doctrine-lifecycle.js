const BUILD='kairos-doctrine-lifecycle-ui-20260714-1';
let record=null,working=false;
document.addEventListener('click',event=>{const opener=event.target.closest?.('[data-child="doctrine-vault"]');if(opener)setTimeout(openPanel,80);const action=event.target.closest?.('[data-doctrine-lifecycle-action]');if(action){event.preventDefault();run(action.dataset.doctrineLifecycleAction)}} ,true);
window.addEventListener('kairos:doctrine-vault:open',()=>setTimeout(openPanel,50));
async function openPanel(){const workspace=document.querySelector('#workspace');if(!workspace)return;let panel=document.querySelector('#doctrine-lifecycle-panel');if(!panel){panel=document.createElement('section');panel.id='doctrine-lifecycle-panel';panel.className='job doctrine-lifecycle-panel';workspace.appendChild(panel)}await loadLatest();render(panel);panel.scrollIntoView({behavior:'smooth',block:'start'})}
async function loadLatest(){try{const r=await request('/api/doctrine-lifecycle/latest');if(r.response.ok)record=r.body.review||null}catch{}}
function render(panel=document.querySelector('#doctrine-lifecycle-panel')){if(!panel)return;const status=record?.status||'not-started';panel.innerHTML=`<p class="eyebrow">Doctrine Vault · Authority Lifecycle</p><h3>Doctrine Review, Supersession, and Retirement</h3><p>Preserve governing authority, applicability, conflicts, and successor lineage. No doctrine is silently edited, superseded, retired, or published.</p><div class="stage-row"><span class="stage ${record?'done':'active'}">1 · Review</span><span class="stage ${record?.decision?'done':record?'active':''}">2 · Approve</span><span class="stage ${record?.implementation?'done':record?.decision?'active':''}">3 · Record Receipt</span></div><p class="status-line"><strong>Status:</strong> ${escapeHTML(status)}</p>${record?recordView():reviewForm()}${working?'<p class="status-line"><i class="spinner"></i>Recording governed doctrine lifecycle evidence…</p>':''}`}
function reviewForm(){return `<div class="summary-card"><label>Doctrine reference</label><input id="dl-reference" placeholder="Canonical doctrine ID or Vault reference"><label>Doctrine title</label><input id="dl-title" placeholder="Doctrine title"><label>Review actor</label><input id="dl-review-actor" placeholder="Authorized reviewer"><label>Verified review evidence</label><textarea id="dl-review-evidence" placeholder="Authoritative evidence supporting this review"></textarea><label>Current applicability</label><textarea id="dl-applicability" placeholder="Where this doctrine currently governs"></textarea><label>Conflicts or obsolete provisions</label><textarea id="dl-conflicts" placeholder="Known conflicts, overlaps, or obsolete provisions"></textarea><label>Review trigger</label><textarea id="dl-trigger" placeholder="Why this review is required now"></textarea><div class="job-actions"><button class="primary" data-doctrine-lifecycle-action="open">Open Approval-Gated Review</button></div></div>`}
function recordView(){const decision=record.decision,implementation=record.implementation;return `<div class="summary-card"><h4>${escapeHTML(record.title||'Doctrine')}</h4><p><strong>Reference:</strong> ${escapeHTML(record.doctrineReference||'—')}</p><p><strong>Applicability:</strong> ${escapeHTML(record.applicability||'—')}</p><p><strong>Conflicts:</strong> ${escapeHTML(record.conflicts||'None recorded')}</p></div>${!decision?decisionForm():`<div class="summary-card"><h4>Approved lifecycle decision</h4><p>${escapeHTML(decision.decision)} · ${escapeHTML(decision.rationale)}</p><p>Successor: ${escapeHTML(decision.successorReference||'Not applicable')}</p></div>`}${decision&&!implementation?receiptForm():implementation?`<div class="deliverable"><strong>Doctrine lifecycle receipt recorded.</strong><p>Vault reference: ${escapeHTML(implementation.vaultReference)} · Canonical predecessor preserved.</p></div>`:''}`}
function decisionForm(){return `<div class="summary-card"><label>Decision</label><select id="dl-decision"><option value="retain">Retain</option><option value="revise">Revise</option><option value="supersede">Supersede</option><option value="retire">Retire</option></select><label>Approval actor</label><input id="dl-approval-actor" placeholder="Authorized approver"><label>Approval evidence</label><textarea id="dl-approval-evidence" placeholder="Completed workflow and approval evidence"></textarea><label>Rationale</label><textarea id="dl-rationale" placeholder="Reason for the approved lifecycle decision"></textarea><label>Successor reference</label><input id="dl-successor" placeholder="Required for revise or supersede"><label>Effective date</label><input id="dl-effective" type="datetime-local"><div class="job-actions"><button class="primary" data-doctrine-lifecycle-action="approve">Approve Bounded Lifecycle Decision</button></div></div>`}
function receiptForm(){return `<div class="summary-card"><label>Implementation actor</label><input id="dl-implementation-actor" placeholder="Doctrine Vault operator"><label>Verified receipt evidence</label><textarea id="dl-receipt-evidence" placeholder="Evidence that the approved Vault action was recorded"></textarea><label>Doctrine Vault reference</label><input id="dl-vault-reference" placeholder="Final Vault record reference"><label>Communication boundary</label><textarea id="dl-communication-boundary" placeholder="What may or may not be communicated externally"></textarea><div class="job-actions"><button class="primary" data-doctrine-lifecycle-action="receipt">Record Verified Doctrine Receipt</button></div></div>`}
async function run(action){if(working)return;working=true;render();try{let url='/api/doctrine-lifecycle',method='POST',payload={};if(action==='open')payload={doctrineReference:value('dl-reference'),title:value('dl-title'),reviewActor:value('dl-review-actor'),reviewEvidence:value('dl-review-evidence'),applicability:value('dl-applicability'),conflicts:value('dl-conflicts'),reviewTrigger:value('dl-trigger')};if(action==='approve'){url+=`/${encodeURIComponent(record.id)}/approve`;method='PATCH';payload={decision:value('dl-decision'),approvalActor:value('dl-approval-actor'),approvalEvidence:value('dl-approval-evidence'),rationale:value('dl-rationale'),successorReference:value('dl-successor'),effectiveAt:value('dl-effective')}}if(action==='receipt'){url+=`/${encodeURIComponent(record.id)}/receipt`;method='PATCH';payload={implementationActor:value('dl-implementation-actor'),receiptEvidence:value('dl-receipt-evidence'),vaultReference:value('dl-vault-reference'),communicationBoundary:value('dl-communication-boundary')}}const r=await request(url,{method,headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:JSON.stringify(payload)});if(!r.response.ok)throw new Error(r.body?.error?.message||'Doctrine lifecycle operation failed.');record=r.body.review;render()}catch(error){alert(error.message)}finally{working=false;render()}}
function value(id){return document.getElementById(id)?.value?.trim()||''}async function request(url,init={}){const response=await fetch(url,{cache:'no-store',credentials:'include',...init});const text=await response.text();let body={};try{body=text?JSON.parse(text):{}}catch{body={message:text}}return{response,body}}function escapeHTML(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
window.KairosDoctrineLifecycle={build:BUILD,open:openPanel};