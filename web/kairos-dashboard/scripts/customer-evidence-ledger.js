import'./customer-evidence-offer-learning-loop.js';const BUILD='kairos-customer-evidence-ledger-ui-20260714-1';let current=null;document.addEventListener('click',event=>{const trigger=event.target.closest?.('[data-open-customer-evidence]');if(!trigger)return;event.preventDefault();event.stopImmediatePropagation();openWorkspace()});new MutationObserver(attach).observe(document.documentElement,{subtree:true,childList:true});attach();function attach(){const host=document.querySelector('#customer-journeys .journey-actions');if(!host||host.querySelector('[data-open-customer-evidence]'))return;const button=document.createElement('button');button.type='button';button.dataset.openCustomerEvidence='';button.textContent='Open Customer Evidence Ledger';host.appendChild(button);window.KairosCustomerEvidenceOfferLearning?.attach?.()}async function openWorkspace(){const customerReference=prompt('Authenticated customer or project reference');if(!customerReference)return;const actor=prompt('Ledger actor');const consentEvidence=prompt('Verified consent and authorization evidence');const purpose=prompt('Bounded longitudinal evidence purpose');const domain=prompt('Source domain: visitor-activity, customer-portal, deliverables, customer-journey, or support-intelligence');const reference=prompt('Verified source record reference');const evidence=prompt('Verified source evidence');const authoritativeSource=prompt('Authoritative source');const response=await api('/api/customer-evidence-ledgers',{method:'POST',body:{customerReference,actor,consentEvidence,purpose,sourceRecords:[{domain,reference,evidence,authoritativeSource}]}});current=response.ledger;alert(`Customer evidence ledger ${current.id} created. Complete and approve workflow ${current.workflowID}, then certify the ledger before adoption.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,cache:'no-store',credentials:'include',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Customer evidence operation failed.');return data}window.KairosCustomerEvidenceLedger={build:BUILD,attach,open:openWorkspace,getCurrent:()=>current};