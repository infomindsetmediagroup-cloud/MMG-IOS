import{getCommandRefresh}from'./command-refresh-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-command-refresh-panel]'))return;const r=getCommandRefresh();const card=document.createElement('article');card.className='card full';card.dataset.commandRefreshPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Refresh</p><h3>Command Refresh</h3></div><span class="${r.ready?'badge good':'badge warning'}">${e(r.status)}</span></div><p class="muted" style="margin-top:12px;">${e(r.summary)} • ${e(r.updated)}</p><div class="list" style="margin-top:16px;"><div class="list-item"><div><strong>${e(r.action)}</strong><p class="muted">Recommended next refresh action</p></div><span class="badge">Next</span></div></div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);