import{getMissionControl}from'./mission-control-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-mission-control-panel]'))return;const m=getMissionControl();const card=document.createElement('article');card.className='card full';card.dataset.missionControlPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Mission</p><h3>Mission Control</h3></div><span class="${m.status==='EXECUTE'?'badge good':'badge warning'}">${e(m.status)}</span></div><p class="muted" style="margin-top:12px;">${e(m.headline)} • ${e(m.updated)}</p><div class="list" style="margin-top:16px;">${m.tasks.map((i,n)=>`<div class="list-item"><strong>${n+1}. ${e(i)}</strong><span class="badge">${n?'Task':'Command'}</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);