import{getExecutionFocus}from'./execution-focus-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-execution-focus-panel]'))return;const f=getExecutionFocus();const card=document.createElement('article');card.className='card full';card.dataset.executionFocusPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Focus</p><h3>Execution Focus</h3></div><span class="badge good">${e(f.urgency)}</span></div><div class="list" style="margin-top:16px;">${f.queue.map((i,n)=>`<div class="list-item"><div><strong>${n+1}. ${e(i)}</strong><p class="muted">Focus queue • Score ${e(f.score)} • ${e(f.updated)}</p></div><span class="badge">${n?'Next':'Now'}</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);