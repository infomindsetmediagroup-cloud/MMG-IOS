import{getValidationChecklist}from'./validation-checklist-engine.js';function e(v){return String(v||'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-validation-checklist-panel]'))return;const c=getValidationChecklist();const card=document.createElement('article');card.className='card full';card.dataset.validationChecklistPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Validation</p><h3>Validation Checklist</h3></div><span class="${c.ready?'badge good':'badge warning'}">${c.ready?'Ready':'Review'}</span></div><div class="list" style="margin-top:16px;">${c.items.map(i=>`<div class="list-item"><strong>${e(i.name)}</strong><span class="${i.done?'badge good':'badge warning'}">${i.done?'Done':'Open'}</span></div>`).join('')}</div><p class="muted" style="margin-top:12px;">${e(c.next)} • ${e(c.updated)}</p>`;v.prepend(card)}window.addEventListener('DOMContentLoaded',render);window.addEventListener('kairos:auth',render);