import'./growth-offer-strategy-loop.js';const BUILD='kairos-growth-commercial-activation-ui-20260715-2';let attached=false;new MutationObserver(attach).observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('kairos:growth-plan:open',()=>setTimeout(attach,120));attach();function attach(){const host=document.querySelector('#growth-plan .growth-actions');if(!host||host.querySelector('[data-growth-commercial-activation]'))return;const b=document.createElement('button');b.dataset.growthCommercialActivation='';b.textContent='Activate Verified Growth Direction';b.addEventListener('click',open);host.appendChild(b);attached=true;window.KairosGrowthOfferStrategy?.attach?.()}async function open(){const portfolioID=prompt('Growth portfolio review ID');if(!portfolioID)return;const track=(prompt('Activation track: offer or campaign')||'').trim().toLowerCase();if(!['offer','campaign'].includes(track))return alert('Track must be offer or campaign.');const targetReference=prompt(track==='offer'?'Certified Offer Builder ID':'Approved Campaign Operations ID');const actor=prompt('Activation actor');const evidence=prompt('Verified activation evidence');const activationObjective=prompt('Bounded activation objective');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');try{const data=await api('/api/growth-commercial-activations',{method:'POST',body:{portfolioID,track,targetReference,actor,evidence,activationObjective,successMetric,stopCondition}});alert(`Activation ${data.record.id} created. Complete and approve workflow ${data.workflow.id}. No execution occurs automatically.`)}catch(e){alert(e.message)}}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,cache:'no-store',credentials:'include',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Growth commercial activation failed.');return data}window.KairosGrowthCommercialActivation={build:BUILD,attach,get attached(){return attached}};