import'./knowledge-library-authority-lifecycle.js';const observer=new MutationObserver(attach);observer.observe(document.documentElement,{subtree:true,childList:true});document.addEventListener('click',()=>setTimeout(attach,80),true);attach();function attach(){const host=document.querySelector('#knowledge-library-provenance .knowledge-actions')||document.querySelector('#knowledge-library-provenance .knowledge-package');if(!host||host.querySelector('[data-open-knowledge-authority]'))return;const button=document.createElement('button');button.type='button';button.dataset.openKnowledgeAuthority='';button.textContent='Review Source Authority Lifecycle';button.addEventListener('click',()=>window.KairosKnowledgeAuthority?.open?.());host.appendChild(button)}