const BUILD='kairos-manuscript-edition-ui-20260714-1';new MutationObserver(attach).observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('kairos:manuscript:restore',()=>setTimeout(attach,120));attach();function attach(){const panel=document.querySelector('#manuscript-studio-overlay .manuscript-panel');if(!panel||panel.querySelector('[data-manuscript-edition-lifecycle]'))return;const section=document.createElement('section');section.dataset.manuscriptEditionLifecycle='';section.className='manuscript-edition-lifecycle';section.innerHTML=`<div><p class="eyebrow">Certified Edition Governance</p><h3>Manuscript Edition Lifecycle</h3><p>Review a certified edition, preserve the predecessor, and authorize a verified Publishing Studio successor handoff. No source replacement, platform submission, or publication occurs automatically.</p></div><button type="button" class="secondary" data-open-edition-review>Review Certified Edition</button>`;panel.appendChild(section);section.querySelector('button').onclick=open}async function open(){const manuscriptID=prompt('Certified manuscript ID');if(!manuscriptID)return;const title=prompt('Manuscript title')||'Untitled manuscript';const actor=prompt('Review actor');const certificationEvidence=prompt('Certification evidence');const reviewEvidence=prompt('Current review evidence');const assessment=prompt('Edition assessment');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const response=await api('/api/manuscript-edition-reviews',{method:'POST',body:{manuscriptID,title,actor,certificationStatus:'manuscript-certified',certificationEvidence,reviewEvidence,assessment,successMetric,stopCondition}});alert(`Edition review ${response.record.id} created. Complete and approve workflow ${response.record.workflowID} before recording the lifecycle decision and Publishing Studio handoff.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Manuscript edition operation failed.');return data}window.KairosManuscriptEditionLifecycle={build:BUILD,attach};