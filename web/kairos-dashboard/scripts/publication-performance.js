const BUILD="kairos-publication-performance-ui-20260713-1";
const ACTIVE_KEY="kairos.production.active-workspace";
let state={record:null,busy:false,error:""};

async function enhance(){
  const operations=document.querySelector("#publication-operations");
  if(!operations||document.querySelector("#publication-performance"))return;
  const projectId=activeProjectId();if(!projectId)return;
  const section=document.createElement("section");section.id="publication-performance";section.className="publication-performance";section.innerHTML='<p class="eyebrow">Performance & royalties</p><h3>Loading Verified Ledger…</h3>';operations.insertAdjacentElement("afterend",section);await load(projectId);
}
async function load(id){state.busy=true;state.error="";render(id);try{const r=await fetch(`/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-performance`,{credentials:"include",cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Performance ledger could not be loaded.");state.record=b.performance;}catch(e){state.error=e?.message||"Performance ledger could not be loaded.";}finally{state.busy=false;render(id);}}
function render(id){const s=document.querySelector("#publication-performance");if(!s)return;if(state.busy){s.innerHTML='<p class="eyebrow">Performance & royalties</p><h3>Updating verified ledger…</h3>';return;}if(state.error){s.innerHTML=`<p class="eyebrow">Performance & royalties</p><h3>Ledger needs attention</h3><p class="manuscript-error">${esc(state.error)}</p><button class="secondary" data-perf-retry>Retry</button>`;s.querySelector("[data-perf-retry]")?.addEventListener("click",()=>load(id));return;}const r=state.record||{entries:[],totals:{}};const t=r.totals||{};s.innerHTML=`<p class="eyebrow">Performance & royalties</p><h3>Verified publication ledger</h3><p>Only confirmed platform statements and payout evidence count toward totals. Unverified entries remain excluded.</p><div class="publication-performance-summary"><span><strong>${Number(t.units||0).toLocaleString()}</strong><small>verified units</small></span><span><strong>${money(t.grossRevenue,r.currency)}</strong><small>gross revenue</small></span><span><strong>${money(t.netRoyalty,r.currency)}</strong><small>net royalties</small></span><span><strong>${Number(r.unverifiedEntryCount||0)}</strong><small>awaiting verification</small></span></div><details><summary>Add statement entry</summary><div class="manuscript-grid"><label>Entry type<select data-perf-type><option value="sales-statement">Sales statement</option><option value="royalty-statement">Royalty statement</option><option value="payout">Payout</option><option value="adjustment">Adjustment</option><option value="return-statement">Return statement</option></select></label><label>Platform<input data-perf-platform maxlength="120"></label></div><div class="manuscript-grid"><label>Period start<input data-perf-start type="date"></label><label>Period end<input data-perf-end type="date"></label></div><div class="manuscript-grid"><label>Units<input data-perf-units type="number" min="0" step="1" value="0"></label><label>Returns<input data-perf-returns type="number" min="0" step="1" value="0"></label></div><div class="manuscript-grid"><label>Gross revenue<input data-perf-gross type="number" min="0" step="0.01" value="0"></label><label>Platform fees<input data-perf-fees type="number" min="0" step="0.01" value="0"></label></div><div class="manuscript-grid"><label>Tax withheld<input data-perf-tax type="number" min="0" step="0.01" value="0"></label><label>Net royalty<input data-perf-net type="number" min="0" step="0.01" value="0"></label></div><div class="manuscript-grid"><label>Statement source<input data-perf-source maxlength="180" placeholder="KDP royalty report"></label><label>Statement reference<input data-perf-reference maxlength="240"></label></div><label>Evidence note<textarea data-perf-note maxlength="4000"></textarea></label><button class="primary" data-perf-add>Add Unverified Entry</button></details><a class="manuscript-package" href="/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-performance/export" download>Download Ledger</a>${entryList(r.entries||[])}`;s.querySelector("[data-perf-add]")?.addEventListener("click",()=>add(id));s.querySelectorAll("[data-perf-verify]").forEach(b=>b.addEventListener("click",()=>verify(id,b.dataset.perfVerify)));s.querySelectorAll("[data-perf-void]").forEach(b=>b.addEventListener("click",()=>voidEntry(id,b.dataset.perfVoid));}
function entryList(entries){if(!entries.length)return'<p class="manuscript-note">No statements recorded yet.</p>';return`<div class="issue-list publication-performance-list">${entries.slice().reverse().map(e=>`<article><b>${esc(e.type)} · ${esc(e.status)}</b><p>${esc(e.periodStart)}–${esc(e.periodEnd)} · ${Number(e.units||0)} units · ${money(e.netRoyalty,e.currency)}</p><small>${esc(e.evidence?.source||"")} · ${esc(e.evidence?.reference||"")}</small><div class="manuscript-actions">${e.status==="unverified"?`<button class="secondary" data-perf-verify="${esc(e.entryId)}">Verify</button>`:""}${e.status!=="void"?`<button class="secondary" data-perf-void="${esc(e.entryId)}">Void</button>`:""}</div></article>`).join("")}</div>`;}
async function add(id){const q=x=>document.querySelector(x);await run(id,"entries",{type:q("[data-perf-type]")?.value,platform:q("[data-perf-platform]")?.value,periodStart:q("[data-perf-start]")?.value,periodEnd:q("[data-perf-end]")?.value,units:Number(q("[data-perf-units]")?.value||0),returns:Number(q("[data-perf-returns]")?.value||0),grossRevenue:Number(q("[data-perf-gross]")?.value||0),platformFees:Number(q("[data-perf-fees]")?.value||0),taxWithheld:Number(q("[data-perf-tax]")?.value||0),netRoyalty:Number(q("[data-perf-net]")?.value||0),source:q("[data-perf-source]")?.value,reference:q("[data-perf-reference]")?.value,evidenceNote:q("[data-perf-note]")?.value,reportedAt:new Date().toISOString().slice(0,10),actor:"Executive"});}
async function verify(id,entryId){const note=window.prompt("Verification note (optional)")||"";await run(id,`verify/${encodeURIComponent(entryId)}`,{note,actor:"Executive"});}
async function voidEntry(id,entryId){const reason=window.prompt("Reason for voiding this ledger entry")||"";if(!reason)return;await run(id,`void/${encodeURIComponent(entryId)}`,{reason,actor:"Executive"});}
async function run(id,path,payload){state.busy=true;state.error="";render(id);try{const r=await fetch(`/api/production-registry/manuscripts/${encodeURIComponent(id)}/publication-performance/${path}`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json","X-MMG-Client-Build":BUILD},body:JSON.stringify(payload)});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||"Performance action failed.");state.record=b.performance;await window.KairosProductionWorkspace?.refresh?.();}catch(e){state.error=e?.message||"Performance action failed.";}finally{state.busy=false;render(id);}}
function activeProjectId(){try{const a=JSON.parse(sessionStorage.getItem(ACTIVE_KEY)||"null");return a?.workspace==="manuscript-studio"?a.projectId||null:null;}catch{return null;}}function money(v,c="USD"){try{return new Intl.NumberFormat(undefined,{style:"currency",currency:c||"USD"}).format(Number(v||0));}catch{return`${c||"USD"} ${Number(v||0).toFixed(2)}`;}}function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);}
new MutationObserver(enhance).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener("kairos:production:state-changed",enhance);enhance();
