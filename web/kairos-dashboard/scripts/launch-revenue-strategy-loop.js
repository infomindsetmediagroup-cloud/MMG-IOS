const BUILD='kairos-launch-revenue-strategy-ui-20260715-1';new MutationObserver(attach).observe(document.documentElement,{subtree:true,childList:true});document.addEventListener('click',e=>{if(e.target.closest?.('[data-child="product-launch"]'))setTimeout(attach,120)});attach();function attach(){const host=document.querySelector('#product-launch-studio .launch-studio-actions');if(!host||host.querySelector('[data-launch-revenue-strategy]'))return;const b=document.createElement('button');b.dataset.launchRevenueStrategy='';b.textContent='Measure Commercial Outcome';b.addEventListener('click',open);host.appendChild(b)}async function open(){const launchPortfolioID=prompt('Closed Product Launch portfolio ID');if(!launchPortfolioID)return;const revenueDecisionID=prompt('Approved Revenue Intelligence decision ID');const actor=prompt('Review actor');const evidence=prompt('Verified launch and revenue evidence');const commercialObjective=prompt('Bounded commercial objective');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');try{const data=await api('/api/launch-revenue-mandates',{method:'POST',body:{launchPortfolioID,revenueDecisionID,actor,evidence,commercialObjective,successMetric,stopCondition}});alert(`Launch revenue mandate ${data.record.id} created. Complete and approve workflow ${data.workflow.id}. No execution occurs automatically.`)}catch(e){alert(e.message)}}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,cache:'no-store',credentials:'include',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Launch revenue strategy operation failed.');return data}window.KairosLaunchRevenueStrategy={build:BUILD,attach};