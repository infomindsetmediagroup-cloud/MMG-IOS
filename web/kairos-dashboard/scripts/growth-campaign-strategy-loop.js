const BUILD='kairos-growth-campaign-strategy-ui-20260714-1';new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{if(event.target.closest?.('[data-child="growth-plan"],[data-child="campaign-operations"]'))setTimeout(attach,100)});attach();function attach(){for(const selector of['#growth-plan-workspace','.growth-plan-workspace','#campaign-operations-workspace','.campaign-operations-workspace']){const host=document.querySelector(selector);if(!host||host.querySelector('[data-growth-campaign-strategy]'))continue;const section=document.createElement('section');section.dataset.growthCampaignStrategy='';section.innerHTML=`<div class="governed-boundary"><strong>Growth Plan → Campaign Operations</strong><p>Convert an approved Growth Plan portfolio direction into a bounded campaign mandate, then return verified campaign learning to the parent plan. No budget, channel, schedule, claims, customer-facing, or publication change occurs automatically.</p><button type="button" data-open-growth-campaign>Open Strategy Handoff</button></div>`;host.appendChild(section);section.querySelector('button').addEventListener('click',open)}}async function open(){const growthPlanID=prompt('Growth Plan ID');if(!growthPlanID)return;const portfolioDecisionID=prompt('Approved Growth Plan portfolio decision ID');const actor=prompt('Review actor');const evidence=prompt('Verified strategy evidence');const objective=prompt('Bounded campaign objective');const audience=prompt('Audience');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const data=await api('/api/growth-campaign-mandates',{method:'POST',body:{growthPlanID,portfolioDecisionID,actor,evidence,objective,audience,successMetric,stopCondition}});alert(`Campaign mandate ${data.record.id} created. Complete and approve workflow ${data.record.workflowID}, then record the verified outcome and Growth Plan adoption receipt.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Growth campaign strategy operation failed.');return data}window.KairosGrowthCampaignStrategy={build:BUILD,attach};