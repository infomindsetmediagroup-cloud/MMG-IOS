import{getOperatorRunbook}from'./operator-runbook.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-runbook-panel]'))return;const r=getOperatorRunbook();const card=document.createElement('article');card.className='card full';card.dataset.runbookPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Runbook</p><h3>${e(r.title)}</h3></div><span class="badge good">${e(r.status)}</span></div><div class="list" style="margin-top:16px;">${r.steps.map((s,i)=>`<div class="list-item"><div><strong>${i+1}. ${e(s)}</strong><p class="muted">Operator sequence</p></div><span class="badge">Step</span></div>`).join('')}</div>`;v.prepend(card)}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);