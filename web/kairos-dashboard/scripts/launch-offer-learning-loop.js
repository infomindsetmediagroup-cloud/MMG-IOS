const BUILD='kairos-launch-offer-learning-ui-20260715-1';new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest?.('[data-child="offer-builder"]'))setTimeout(attach,100)});attach();function attach(){for(const selector of['#offer-builder','.offer-builder']){const host=document.querySelector(selector);if(!host||host.querySelector('[data-launch-offer-learning]'))continue;const section=document.createElement('section');section.dataset.launchOfferLearning='';section.innerHTML=`<div class="governed-boundary"><strong>Product Launch → Offer Builder Learning</strong><p>Use a closed launch portfolio receipt to approve retain, scale, revise, or retire direction for the governed offer. No pricing, offer, claims, launch, customer-facing, or publication change occurs automatically.</p><button type="button" data-open-launch-offer-learning>Open Offer Learning Review</button></div>`;host.appendChild(section);section.querySelector('button').addEventListener('click',open)}}async function open(){const launchPortfolioID=prompt('Closed Product Launch portfolio ID');if(!launchPortfolioID)return;const offerMandateID=prompt('Governed Offer mandate ID');const actor=prompt('Review actor');const evidence=prompt('Verified launch and offer evidence');const assessment=prompt('Offer learning assessment');const successMetric=prompt('Success metric');const stopCondition=prompt('Stop condition');const data=await api('/api/launch-offer-learning',{method:'POST',body:{launchPortfolioID,offerMandateID,actor,evidence,assessment,successMetric,stopCondition}});alert(`Offer learning review ${data.record.id} created. Complete and approve workflow ${data.record.workflowID}, then record the verified adoption receipt.`)}async function api(url,{method='GET',body}={}){const response=await fetch(url,{method,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','X-MMG-Client-Build':BUILD},body:body?JSON.stringify(body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data?.error?.message||'Launch offer learning operation failed.');return data}window.KairosLaunchOfferLearning={build:BUILD,attach};