import{getLaunchControl,saveLaunchCheckpoint,getLaunchCheckpoints}from'./launch-control-engine.js';function e(v){return String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function render(){const v=document.querySelector('#dashboard-view');if(!v||v.querySelector('[data-launch-control-panel]'))return;const l=getLaunchControl();const c=getLaunchCheckpoints();const card=document.createElement('article');card.className='card full';card.dataset.launchControlPanel='true';card.innerHTML=`<div class="card-header"><div><p class="eyebrow">Launch</p><h3>Launch Control</h3></div><span class="${l.status==='Ready'?'badge good':'badge warning'}">${e(l.status)}</span></div><p class="muted" style="margin-top:12px;">${e(l.reason)} • ${e(l.summary)}</p><div class="action-row"><button class="action-button" data-save-launch>Save Checkpoint</button></div><div class="list" style="margin-top:16px;">${(c.length?c:[{note:'No launch checkpoints yet',updated:'Pending',status:'Standby'}]).slice(0,6).map(i=>`<div class="list-item"><div><strong>${e(i.note)}</strong><p class="muted">${e(i.updated)}</p></div><span class="badge">${e(i.status)}</span></div>`).join('')}</div>`;v.prepend(card);card.querySelector('[data-save-launch]').addEventListener('click',()=>{saveLaunchCheckpoint();card.remove();render()})}const o=new MutationObserver(render);window.addEventListener('DOMContentLoaded',()=>{const v=document.querySelector('#dashboard-view');if(v)o.observe(v,{childList:true});render()});window.addEventListener('kairos:auth',render);