import{getSecurityConstitution}from'./security-constitution-engine.js';export function getSecurityGate(){const s=getSecurityConstitution();return{priority:s.priority,ready:false,status:'Security review required',summary:'Production release remains blocked until P1 security gates are reviewed and cleared.',checks:s.gates.map(g=>({name:g,passed:false})),updated:new Date().toLocaleString()};}