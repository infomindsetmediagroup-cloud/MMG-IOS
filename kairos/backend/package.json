{
  "name": "kairos-backend-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "certify": "npm run build && npm test",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@types/node": "latest",
    "express": "latest",
    "openai": "latest",
    "tsx": "latest",
    "typescript": "latest"
  },
  "devDependencies": {}
}
