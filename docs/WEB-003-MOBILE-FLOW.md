# WEB-003 Mobile Flow

The mobile Website Production overlay now presents a compact website-job selector, the selected job summary, and the production prompt immediately. The previous long stack of action cards is hidden on small screens. The floating Website Map control is suppressed while the production overlay is open.
