name: Verify Canonical MMG Homepage

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Verify GitHub runner startup
        run: echo "Canonical homepage verification runner is available."
