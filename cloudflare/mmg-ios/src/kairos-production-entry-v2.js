import runtime,{KairosProject}from"./kairos-production-entry-v1.js";
import{completeApprovedWorkDispatch,dispatchApprovedBriefingItem,readApprovedWorkDispatch,readApprovedWorkReceipt}from"./kairos-approved-work-dispatcher-v1.js";
import{runApprovedWebsiteExecution}from"./kairos-approved-website-executor-v1.js";
import{prepareExecutiveCorrection,readExecutiveCorrection,resubmitExecutiveCorrection}from"./kairos-executive-correction-loop-v1.js";
import{decideSocialPackage,prepareSocialPackage,readLatestSocialPackage,readSocialPackage}from"./kairos-social-production-v1.js";
import{createTask,createWorkflow,listWorkflows,readWorkflow,updateTask,updateWorkflow}from"./kairos-workflow-runtime-v1.js";
import{dispatchObjective,routeObjective}from"./kairos-objective-router-v1.js";
import{createCreativeProject,readCreativeProject,readLatestCreativeProject}from"./kairos-creative-studio-v1.js";
import{createPublishingProject,readLatestPublishingProject,readPublishingProject}from"./kairos-publishing-studio-v1.js";
import{createLaunchProject,readLatestLaunchProject,readLaunchProject}from"./kairos-product-launch-studio-v1.js";
import{readLatestRevenueReview,readRevenueReview,runRevenueReview}from"./kairos-revenue-intelligence-v1.js";
import{createGrowthPlan,readGrowthPlan,readLatestGrowthPlan}from"./kairos-growth-plan-v1.js";
import{createOffer,readLatestOffer,readOffer}from"./kairos-offer-builder-v1.js";
import{createCampaign,readCampaign,readLatestCampaign}from"./kairos-campaign-operations-v1.js";
import{readLatestVisitorReview,readVisitorReview,runVisitorReview}from"./kairos-visitor-activity-v1.js";
import{createJourney,readJourney,readLatestJourney}from"./kairos-customer-journey-v1.js";
import{createCustomerProject,readCustomerProject,readLatestCustomerProject,updateCustomerProject}from"./kairos-customer-portal-v1.js";
import{createDeliverable,readDeliverable,readLatestDeliverable,updateDeliverable}from"./kairos-deliverables-v1.js";
const BUILD="kairos-production-entry-20260713-20";export{KairosProject};
export default{async fetch(request,env,ctx){const url=new URL(request.url);try{
if(request.method==="POST"&&url.pathname==="/api/deliverables"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createDeliverable(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/deliverables/latest"){const result=await readLatestDeliverable(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="PATCH"&&url.pathname.startsWith("/api/deliverables/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await updateDeliverable(request,id,payload)})}
if(request.method==="GET"&&url.pathname.startsWith("/api/deliverables/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readDeliverable(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/customer-projects"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createCustomerProject(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/customer-projects/latest"){const result=await readLatestCustomerProject(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="PATCH"&&url.pathname.startsWith("/api/customer-projects/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await updateCustomerProject(request,id,payload)})}
if(request.method==="GET"&&url.pathname.startsWith("/api/customer-projects/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readCustomerProject(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/customer-journeys"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createJourney(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/customer-journeys/latest"){const result=await readLatestJourney(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/customer-journeys/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readJourney(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/visitor-activity/reviews"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await runVisitorReview(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/visitor-activity/latest"){const report=await readLatestVisitorReview(request);return report?json({status:"completed",build:BUILD,report}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/visitor-activity/reviews/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const report=await readVisitorReview(request,id);return report?json({status:"completed",build:BUILD,report}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/campaigns"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createCampaign(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/campaigns/latest"){const result=await readLatestCampaign(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/campaigns/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readCampaign(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/offers"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createOffer(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/offers/latest"){const result=await readLatestOffer(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/offers/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readOffer(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/growth-plans"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createGrowthPlan(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/growth-plans/latest"){const plan=await readLatestGrowthPlan(request);return plan?json({status:"completed",build:BUILD,plan}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/growth-plans/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const plan=await readGrowthPlan(request,id);return plan?json({status:"completed",build:BUILD,plan}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/revenue-intelligence/reviews"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await runRevenueReview(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/revenue-intelligence/latest"){const report=await readLatestRevenueReview(request);return report?json({status:"completed",build:BUILD,report}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/revenue-intelligence/reviews/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const report=await readRevenueReview(request,id);return report?json({status:"completed",build:BUILD,report}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/product-launch/projects"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createLaunchProject(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/product-launch/latest"){const result=await readLatestLaunchProject(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/product-launch/projects/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readLaunchProject(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/publishing-studio/projects"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createPublishingProject(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/publishing-studio/latest"){const result=await readLatestPublishingProject(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/publishing-studio/projects/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readPublishingProject(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/creative-studio/projects"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await createCreativeProject(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/creative-studio/latest"){const result=await readLatestCreativeProject(request);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/creative-studio/projects/")){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const result=await readCreativeProject(request,id);return result?json({status:"completed",build:BUILD,...result}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/objectives/route"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,route:routeObjective(payload)})}
if(request.method==="POST"&&url.pathname==="/api/objectives/dispatch"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,dispatch:await dispatchObjective(request,payload)},201)}
if(request.method==="POST"&&url.pathname==="/api/workflows"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,workflow:await createWorkflow(request,payload)},201)}
if(request.method==="GET"&&url.pathname==="/api/workflows")return json({status:"completed",build:BUILD,workflows:await listWorkflows(request)});
if(request.method==="GET"&&/^\/api\/workflows\/[^/]+$/.test(url.pathname)){const id=decodeURIComponent(url.pathname.split("/").pop()||"");const workflow=await readWorkflow(request,id);return workflow?json({status:"completed",build:BUILD,workflow}):json({status:"not-found",build:BUILD},404)}
if(request.method==="POST"&&/^\/api\/workflows\/[^/]+\/tasks$/.test(url.pathname)){const parts=url.pathname.split("/").filter(Boolean),payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,workflow:await createTask(request,decodeURIComponent(parts[2]),payload)},201)}
if(request.method==="PATCH"&&/^\/api\/workflows\/[^/]+\/tasks\/[^/]+$/.test(url.pathname)){const parts=url.pathname.split("/").filter(Boolean),payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,workflow:await updateTask(request,decodeURIComponent(parts[2]),decodeURIComponent(parts[4]),payload)})}
if(request.method==="PATCH"&&/^\/api\/workflows\/[^/]+$/.test(url.pathname)){const id=decodeURIComponent(url.pathname.split("/").pop()||""),payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,workflow:await updateWorkflow(request,id,payload)})}
if(request.method==="POST"&&url.pathname==="/api/social-production/prepare"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,socialPackage:await prepareSocialPackage(request,payload)})}
if(request.method==="POST"&&url.pathname==="/api/social-production/decide"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,socialPackage:await decideSocialPackage(request,payload)})}
if(request.method==="GET"&&url.pathname==="/api/social-production/latest"){const p=await readLatestSocialPackage(request);return p?json({status:"completed",build:BUILD,socialPackage:p}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/social-production/")){const id=decodeURIComponent(url.pathname.split("/").pop()||""),p=await readSocialPackage(request,id);return p?json({status:"completed",build:BUILD,socialPackage:p}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="POST"&&url.pathname==="/api/executive-briefing/execute"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,workOrder:await dispatchApprovedBriefingItem(request,payload)})}
if(request.method==="POST"&&url.pathname==="/api/executive-briefing/execution/run"){const payload=await safeJSON(request.clone()),result=await runApprovedWebsiteExecution(request,env,payload);return json({status:result.status,build:BUILD,result},result.status==="needs-preparation"?409:200)}
if(request.method==="POST"&&url.pathname==="/api/executive-briefing/execution/complete"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await completeApprovedWorkDispatch(request,payload)})}
if(request.method==="POST"&&url.pathname==="/api/executive-briefing/fix/prepare"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,correction:await prepareExecutiveCorrection(request,payload)})}
if(request.method==="POST"&&url.pathname==="/api/executive-briefing/fix/resubmit"){const payload=await safeJSON(request.clone());return json({status:"completed",build:BUILD,...await resubmitExecutiveCorrection(request,payload)})}
if(request.method==="GET"&&url.pathname.startsWith("/api/executive-briefing/fix/")){const id=decodeURIComponent(url.pathname.split("/").pop()||""),c=await readExecutiveCorrection(request,id);return c?json({status:"completed",build:BUILD,correction:c}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/executive-briefing/execution/")&&url.pathname.endsWith("/receipt")){const parts=url.pathname.split("/").filter(Boolean),id=decodeURIComponent(parts[parts.length-2]||""),receipt=await readApprovedWorkReceipt(request,id);return receipt?json({status:"completed",build:BUILD,receipt}):json({status:"not-ready",build:BUILD},404)}
if(request.method==="GET"&&url.pathname.startsWith("/api/executive-briefing/execution/")){const id=decodeURIComponent(url.pathname.split("/").pop()||""),workOrder=await readApprovedWorkDispatch(request,id);return workOrder?json({status:"completed",build:BUILD,workOrder}):json({status:"not-ready",build:BUILD},404)}
return await runtime.fetch(request,env,ctx)}catch(error){const message=error instanceof Error&&error.message?error.message:"Kairos encountered an unexpected production runtime failure.";return json({status:"failed",build:BUILD,route:url.pathname,error:{code:"production_runtime_failed",message}},500)}}};
async function safeJSON(response){try{return await response.json()}catch{return{}}}function json(value,status=200){return new Response(JSON.stringify(value),{status,headers:{"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store","X-MMG-Runtime":BUILD,"X-Content-Type-Options":"nosniff"}})}
