import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),engine=readFileSync(join(root,'src/kairos-live-analytics-rules-digests-v1.js'),'utf8'),edge=readFileSync(join(root,'src/kairos-production-entry-v29.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics-rules-digests.js'),'utf8'),host=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics.js'),'utf8');for(const marker of['readAnalyticsRules','saveAnalyticsRules','applyAnalyticsRules','createDigestSchedule','updateDigestSchedule','digestSummary','daily','weekly','automaticExternalDelivery:false'])assert.ok(engine.includes(marker),`engine missing ${marker}`);for(const marker of['/api/analytics/live/rules','/api/analytics/live/digests','canonical-live-analytics-rules','canonical-live-analytics-digests','rules,digests'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['Edit alert rules','Create digest','Record delivery','Pause','Resume','America/Los_Angeles'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(host.startsWith("import'./live-analytics-rules-digests.js'"));console.log(JSON.stringify({status:'ready',features:['analytics-alert-rules-escalation','analytics-digest-scheduling'],customThresholds:true,ownerRouting:true,escalationWindows:true,daily:true,weekly:true,deliveryReceipts:true,automaticMutation:false,browser:true,appBridge:true},null,2));