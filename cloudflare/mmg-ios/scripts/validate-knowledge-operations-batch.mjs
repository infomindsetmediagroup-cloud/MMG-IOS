import assert from"node:assert/strict";import{existsSync,readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const files={ui:join(repoRoot,"web/kairos-dashboard/scripts/knowledge-operations.js"),css:join(repoRoot,"web/kairos-dashboard/styles/knowledge-operations.css"),index:join(repoRoot,"web/kairos-dashboard/index.html"),registry:join(workerRoot,"src/kairos-readiness-registry-v1.js")};for(const f of Object.values(files))assert.ok(existsSync(f));const ui=readFileSync(files.ui,"utf8");for(const m of['[data-child="knowledge-library"]','[data-child="doctrine-vault"]',"Doctrine Authority Operations","Register Governing Doctrine","Certify Doctrine Application","Build Traceable Synthesis","/api/doctrine/findings"])assert.ok(ui.includes(m));assert.ok(!readFileSync(files.css,"utf8").includes("position:fixed"));const index=readFileSync(files.index,"utf8"),registry=readFileSync(files.registry,"utf8");assert.ok(index.includes("scripts/knowledge-operations.js")&&index.includes("styles/knowledge-operations.css"));assert.ok(registry.includes('"knowledge-library":78')&&registry.includes('"doctrine-vault":76'));console.log(JSON.stringify({status:"ready",knowledgeLibraryReadiness:78,doctrineVaultReadiness:76,canonicalReadiness:true},null,2));