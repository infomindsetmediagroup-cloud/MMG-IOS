import assert from"node:assert/strict";import{existsSync,readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,".."),repo=resolve(root,"../..");const files={runtime:join(root,"src/kairos-campaign-social-handoff-v1.js"),entry:join(root,"src/kairos-production-entry-v3.js"),ui:join(repo,"web/kairos-dashboard/scripts/campaign-social-handoff.js"),css:join(repo,"web/kairos-dashboard/styles/campaign-social-handoff.css"),index:join(repo,"web/kairos-dashboard/index.html")};for(const p of Object.values(files))assert.ok(existsSync(p),`Campaign social handoff file missing: ${p}`);const runtime=readFileSync(files.runtime,"utf8"),entry=readFileSync(files.entry,"utf8"),ui=readFileSync(files.ui,"utf8"),index=readFileSync(files.index,"utf8");for(const marker of["certifyCampaignActivation","authorizeSocialConnectorHandoff","activation-certified","authorized-for-connector-handoff","externalPublicationAutomatic:false","paidSpendAutomatic:false","scheduleMutationAutomatic:false","publicationReceiptRequired:true","connectorExecutionSeparate:true"])assert.ok(runtime.includes(marker),`Campaign social runtime contract missing: ${marker}`);for(const marker of["certifyCampaignActivation","authorizeSocialConnectorHandoff","certify-activation","connector-handoffs"])assert.ok(entry.includes(marker),`Campaign social route missing: ${marker}`);for(const marker of["Campaign Activation Certification","Social Connector Handoff Authorization","Certify Campaign Activation","Authorize Connector Handoff","No publication, paid spend, schedule mutation, deletion, republication, or performance claim occurs automatically"])assert.ok(ui.includes(marker),`Campaign social UI missing: ${marker}`);assert.ok(index.includes("scripts/campaign-social-handoff.js"));assert.ok(index.includes("styles/campaign-social-handoff.css"));assert.ok(!readFileSync(files.css,"utf8").includes("position:fixed"),"Campaign social handoff must not introduce floating controls.");console.log(JSON.stringify({status:"ready",campaignActivationCertification:true,socialConnectorHandoffAuthorization:true,automaticPublication:false,automaticPaidSpend:false,automaticScheduling:false,publicationReceiptRequired:true},null,2));