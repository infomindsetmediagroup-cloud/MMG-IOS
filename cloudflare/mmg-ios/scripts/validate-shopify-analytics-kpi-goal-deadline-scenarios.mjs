import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-deadline-scenarios.js'),'utf8'),forecast=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-forecast.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics-kpi-goal-deadline-scenarios.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-kpi-goal-deadline-scenarios-20260715-2','shopify-analytics-kpi-portfolio-summary.js','DEADLINES_KEY','Pace against target date','Days remaining','Required pace','Current pace','Deadline status','Conservative','Current pace','Stretch','setDeadline','clearDeadline','calculate'])assert.ok(ui.includes(marker),`Shopify KPI deadline scenarios missing ${marker}`);for(const marker of['.shopify-kpi-goal-deadline-scenarios','.shopify-kpi-deadline-grid','.shopify-kpi-scenario-grid','data-deadline-status=on-pace','data-deadline-status=behind'])assert.ok(css.includes(marker),`Shopify KPI deadline scenario style missing ${marker}`);assert.ok(forecast.startsWith("import'./shopify-analytics-kpi-goal-deadline-scenarios.js'"),'Deadline scenarios must load with KPI forecast');assert.ok(index.includes('shopify-analytics-kpi-goals.js'));assert.ok(!index.includes('scripts/live-analytics.js'));console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-goal-deadline-scenarios',savedDeadlines:true,daysRemaining:true,requiredPace:true,paceComparison:true,scenarios:['conservative','current','stretch'],portfolioSummaryMounted:true,observationalOnly:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));