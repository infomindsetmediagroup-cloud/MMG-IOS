import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),engine=readFileSync(join(root,'src/kairos-support-offer-learning-v1.js'),'utf8'),edge=readFileSync(join(root,'src/kairos-production-entry-v28.js'),'utf8'),prior=readFileSync(join(root,'src/kairos-production-entry-v27.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/support-offer-learning-loop.js'),'utf8'),bridge=readFileSync(join(repo,'web/kairos-dashboard/scripts/growth-offer-strategy-loop.js'),'utf8'),wrangler=readFileSync(join(root,'wrangler.toml'),'utf8'),baseline=JSON.parse(readFileSync(join(root,'production-baseline.json'),'utf8'));for(const m of['openSupportOfferLearning','approveSupportOfferLearning','recordSupportOfferAdoption','closedSupportPortfolioRequired:true','certifiedOfferOutcomeRequired:true','successorOfferRequiredForRevision:true','pricingChangeAutomatic:false','offerMutationAutomatic:false','claimsChangeAutomatic:false','productLaunchAutomatic:false','customerCommunicationAutomatic:false','customerFacingChangeAutomatic:false','externalPublicationAutomatic:false'])assert.ok(engine.includes(m),`engine missing ${m}`);for(const m of["kairos-production-entry-v27.js",'/api/support-offer-learning','X-Kairos-Support-Offer-Learning','async scheduled','runtime.scheduled'])assert.ok(edge.includes(m),`edge missing ${m}`);assert.ok(prior.includes('/api/customer-evidence-offer-learning'));for(const m of['Support Intelligence → Offer Builder','Open Support Learning Review','No pricing, offer, claims, launch, communication, customer-facing, publication, or external execution occurs automatically.'])assert.ok(ui.includes(m),`UI missing ${m}`);assert.ok(bridge.startsWith("import'./support-offer-learning-loop.js'"));assert.ok(wrangler.includes('main = "src/kairos-production-entry-v28.js"'));assert.equal(baseline.baseline,'kairos-production-standard-20260715-8');assert.equal(baseline.worker.entry,'src/kairos-production-entry-v28.js');assert.equal(baseline.approvedExpansion.automaticExecution,false);console.log(JSON.stringify({status:'ready',supportOfferLearning:true,approvalGated:true,successorLineage:true,verifiedAdoption:true,automaticExecution:false,activeEdge:'v28'},null,2));