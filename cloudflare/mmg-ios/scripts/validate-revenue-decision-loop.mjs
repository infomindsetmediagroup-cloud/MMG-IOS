import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,'..'),repo=resolve(root,'../..');const engine=readFileSync(join(root,'src/kairos-revenue-decision-v1.js'),'utf8'),entry=readFileSync(join(root,'src/kairos-production-entry-v4.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/revenue-decision-loop.js'),'utf8'),revenueUI=readFileSync(join(repo,'web/kairos-dashboard/scripts/revenue-intelligence.js'),'utf8'),wrangler=readFileSync(join(root,'wrangler.toml'),'utf8');for(const marker of['createRevenueDecision','certifyRevenueDecision','verifiedMetricsRequired:true','inventedMetrics:false','projectionAutomatic:false','pricingChangeAutomatic:false','spendChangeAutomatic:false','customerFacingChangeAutomatic:false','approvalRequired:true'])assert.ok(engine.includes(marker),`engine missing ${marker}`);for(const marker of['/api/revenue-decisions','certifyRevenueDecision','readLatestRevenueDecision'])assert.ok(entry.includes(marker),`route missing ${marker}`);for(const marker of['Verified Revenue Decision','Create Approval-Gated Decision','No automatic pricing, spending, publication, projection, or customer-facing change'])assert.ok(ui.includes(marker),`ui missing ${marker}`);assert.ok(revenueUI.includes('Record Decision'));assert.ok(revenueUI.includes('revenue-decision-loop.js'));assert.ok(wrangler.includes('kairos-production-entry-v4.js'));console.log(JSON.stringify({status:'ready',verifiedRevenueEvidence:true,approvalGatedDecision:true,uncertaintyPreserved:true,automaticPricing:false,automaticSpend:false,automaticPublication:false},null,2));