import fs from "node:fs";
import assert from "node:assert/strict";
const root=new URL("../../../",import.meta.url);
const read=path=>fs.readFileSync(new URL(path,root),"utf8");
const script=read("web/kairos-dashboard/scripts/research-synthesis-operations.js");
const style=read("web/kairos-dashboard/styles/research-synthesis-operations.css");
const index=read("web/kairos-dashboard/index.html");
const registry=read("cloudflare/mmg-ios/src/kairos-readiness-registry-v1.js");
for(const marker of ["kairos:research-brief:open","kairos:intelligence-synthesis:open","Build Research Brief","Build Executive Synthesis","Open My Work","Record Decision"])assert.ok(script.includes(marker),`missing ${marker}`);
assert.ok(style.includes(".research-synthesis-operations"));
assert.ok(index.includes("research-synthesis-operations.js"));
assert.ok(index.includes("research-synthesis-operations.css"));
assert.ok(registry.includes('"research-brief":75'));
assert.ok(registry.includes('"intelligence-synthesis":75'));
console.log("research and synthesis batch validated");
