import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),edge=readFileSync(join(root,'src/kairos-production-entry-v29.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics.js'),'utf8'),shopify=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/live-analytics.css'),'utf8');for(const marker of['/api/analytics/live','/api/analytics/live/heartbeat','canonical-live-analytics','production-green','refreshIntervalSeconds:15','X-Kairos-Live-Analytics'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['REFRESH_MS=15000','HEARTBEAT_MS=30000','kairosAnalytics','kairos:live-analytics','Browser + app status','/api/analytics/live'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(shopify.startsWith("import'./live-analytics.js'"));assert.ok(shopify.includes('setInterval(loadAnalytics, 15000)'));assert.ok(css.includes('.live-analytics-panel')&&css.includes('.live-analytics-grid'));console.log(JSON.stringify({status:'ready',feature:'live-analytics-unification',browser:true,appBridge:true,shopify:true,readiness:true,deployment:true,refreshSeconds:15},null,2));