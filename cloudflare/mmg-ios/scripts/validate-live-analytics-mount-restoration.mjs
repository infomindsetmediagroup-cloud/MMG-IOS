import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8'),shopify=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics.js'),'utf8'),core=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics.js'),'utf8');assert.ok(!index.includes('scripts/live-analytics.js'),'live analytics must not mount in the main command center shell');assert.ok(!index.includes('shopify-analytics.js'),'Shopify analytics must not mount in the main command center shell');assert.ok(shopify.includes('/api/analytics/shopify'),'backend Shopify analytics capability must remain available');assert.ok(core.includes('/api/analytics/live'),'backend live analytics capability must remain available');console.log(JSON.stringify({status:'ready',feature:'live-analytics-shell-exclusion',mainShellAnalyticsVisible:false,backendAnalyticsPreserved:true,browserShellClean:true,appShellClean:true},null,2));