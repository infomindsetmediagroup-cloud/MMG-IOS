import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),engine=readFileSync(join(root,'src/kairos-live-analytics-digest-execution-v1.js'),'utf8'),edge=readFileSync(join(root,'src/kairos-production-entry-v30.js'),'utf8'),prior=readFileSync(join(root,'src/kairos-production-entry-v29.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics-digest-execution.js'),'utf8'),host=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics-rules-digests.js'),'utf8'),wrangler=readFileSync(join(root,'wrangler.toml'),'utf8');for(const marker of['readDigestRuns','queueDigestRun','updateDigestRun','dueDigestSchedules','digestExecutionSummary','queued','running','completed','failed','verified','automaticExternalDelivery:false'])assert.ok(engine.includes(marker),`engine missing ${marker}`);for(const marker of['/api/analytics/live/digest-runs','/api/analytics/live/digests/due','/queue','(start|complete|fail|verify)','digestExecution','kairos-production-entry-v29.js'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['Digest execution queue','Queue run','Start','Complete','Fail','Verify delivery'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(host.startsWith("import'./live-analytics-digest-execution.js'"));assert.ok(prior.includes('/api/analytics/live/digests')&&prior.includes('/api/analytics/live/rules'));assert.ok(wrangler.includes('main = "src/kairos-production-entry-v30.js"'));console.log(JSON.stringify({status:'ready',feature:'live-analytics-digest-execution',dueDetection:true,queue:true,runStates:true,canonicalPackage:true,deliveryVerification:true,automaticExternalDelivery:false,priorAnalyticsGovernancePreserved:true},null,2));