import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,'..'),repo=resolve(root,'../..');const files={engine:join(root,'src/kairos-doctrine-lifecycle-v1.js'),entry:join(root,'src/kairos-production-entry-v9.js'),prior:join(root,'src/kairos-production-entry-v8.js'),ui:join(repo,'web/kairos-dashboard/scripts/doctrine-lifecycle.js'),knowledge:join(repo,'web/kairos-dashboard/scripts/knowledge-operations.js'),wrangler:join(root,'wrangler.toml')};for(const f of Object.values(files))assert.ok(existsSync(f),`Doctrine lifecycle file missing: ${f}`);const engine=readFileSync(files.engine,'utf8');for(const m of['openDoctrineReview','approveDoctrineDecision','recordDoctrineReceipt','canonicalDoctrineImmutable:true','silentMutationForbidden:true','successorLineageRequired:true','automaticRetirement:false','automaticSupersession:false','inventedAuthority:false','lifecycle-receipt-recorded'])assert.ok(engine.includes(m),`Doctrine engine missing ${m}`);const entry=readFileSync(files.entry,'utf8');for(const m of['kairos-production-entry-v8.js','/api/doctrine-lifecycle','approveDoctrineDecision','recordDoctrineReceipt','async scheduled','runtime.scheduled','X-Kairos-Doctrine-Lifecycle'])assert.ok(entry.includes(m),`Doctrine edge missing ${m}`);const ui=readFileSync(files.ui,'utf8');for(const m of['Doctrine Review, Supersession, and Retirement','Open Approval-Gated Review','Approve Bounded Lifecycle Decision','Record Verified Doctrine Receipt','No doctrine is silently edited'])assert.ok(ui.includes(m),`Doctrine UI missing ${m}`);const knowledge=readFileSync(files.knowledge,'utf8');assert.ok(knowledge.includes("import('./doctrine-lifecycle.js')"));assert.ok(knowledge.includes('Review Supersession or Retirement'));assert.ok(readFileSync(files.wrangler,'utf8').includes('main = "src/kairos-production-entry-v9.js"'));console.log(JSON.stringify({status:'ready',doctrineReview:true,approvedLifecycleDecision:true,immutablePredecessor:true,successorLineage:true,verifiedVaultReceipt:true,activeEdge:'v9'},null,2));