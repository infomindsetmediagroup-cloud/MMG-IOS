import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,'..'),repo=resolve(root,'../..');const files={engine:join(root,'src/kairos-doctrine-lifecycle-v1.js'),entry:join(root,'src/kairos-production-entry-v9.js'),active:join(root,'src/kairos-production-entry-v10.js'),ui:join(repo,'web/kairos-dashboard/scripts/doctrine-lifecycle.js'),knowledge:join(repo,'web/kairos-dashboard/scripts/knowledge-operations.js'),wrangler:join(root,'wrangler.toml')};for(const f of Object.values(files))assert.ok(existsSync(f));const engine=readFileSync(files.engine,'utf8');for(const m of['openDoctrineReview','approveDoctrineDecision','recordDoctrineReceipt','canonicalDoctrineImmutable:true','silentMutationForbidden:true','successorLineageRequired:true','automaticRetirement:false','automaticSupersession:false','inventedAuthority:false','lifecycle-receipt-recorded'])assert.ok(engine.includes(m));const entry=readFileSync(files.entry,'utf8');for(const m of['kairos-production-entry-v8.js','/api/doctrine-lifecycle','approveDoctrineDecision','recordDoctrineReceipt','async scheduled','runtime.scheduled','X-Kairos-Doctrine-Lifecycle'])assert.ok(entry.includes(m));assert.ok(readFileSync(files.active,'utf8').includes('kairos-production-entry-v9.js'));assert.ok(readFileSync(files.ui,'utf8').includes('Doctrine Review, Supersession, and Retirement'));assert.ok(readFileSync(files.knowledge,'utf8').includes('Review Supersession or Retirement'));assert.ok(readFileSync(files.wrangler,'utf8').includes('main = "src/kairos-production-entry-v10.js"'));console.log(JSON.stringify({status:'ready',doctrineReview:true,verifiedVaultReceipt:true,activeEdge:'v10'},null,2));