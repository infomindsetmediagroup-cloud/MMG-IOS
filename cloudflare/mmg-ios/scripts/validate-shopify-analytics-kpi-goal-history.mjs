import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-history.js'),'utf8'),goals=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goals.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics-kpi-goal-history.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-kpi-goal-history-20260715-1','HISTORY_KEY','MAX_ROWS','Progress checkpoints','Recorded checkpoints','Milestones reached','Improving','Declining','Clear history','getHistory','clearMetric','clearAll'])assert.ok(ui.includes(marker),`Shopify KPI goal history missing ${marker}`);for(const marker of['.shopify-kpi-goal-history','.shopify-kpi-history-summary','.shopify-kpi-history-timeline','data-trend=improving','data-trend=declining'])assert.ok(css.includes(marker),`Shopify KPI goal history style missing ${marker}`);assert.ok(goals.startsWith("import'./shopify-analytics-kpi-goal-history.js'"),'Goal history must load with KPI goals');assert.ok(index.includes('shopify-analytics-kpi-goals.js'),'KPI goals must remain mounted');assert.ok(!index.includes('scripts/live-analytics.js'),'internal live analytics must remain excluded');console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-goal-history',localCheckpoints:true,milestones:[25,50,75,100],trendDirection:true,maxRowsPerMetric:30,clearHistory:true,observationalOnly:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));