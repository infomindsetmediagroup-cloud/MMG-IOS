import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-portfolio-summary.js'),'utf8'),deadline=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-deadline-scenarios.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics-kpi-portfolio-summary.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-kpi-portfolio-summary-20260715-1','Executive goal summary','Total goals','Average progress','Needs attention','Priority order','Highest operational risk first','buildPortfolio','getPortfolio','overdue','off-pace','behind'])assert.ok(ui.includes(marker),`Shopify KPI portfolio summary missing ${marker}`);for(const marker of['.shopify-kpi-portfolio-summary','.shopify-kpi-portfolio-grid','.shopify-kpi-portfolio-priority','data-portfolio-status=overdue','data-portfolio-status=behind'])assert.ok(css.includes(marker),`Shopify KPI portfolio summary style missing ${marker}`);assert.ok(deadline.startsWith("import'./shopify-analytics-kpi-portfolio-summary.js'"),'Portfolio summary must load with deadline scenarios');assert.ok(index.includes('shopify-analytics-kpi-goals.js'),'Shopify KPI analytics chain must remain mounted');assert.ok(!index.includes('scripts/live-analytics.js'),'Internal live analytics must remain excluded');console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-portfolio-summary',consolidatedCounts:true,averageProgress:true,riskPriority:true,deadlineAware:true,observationalOnly:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));