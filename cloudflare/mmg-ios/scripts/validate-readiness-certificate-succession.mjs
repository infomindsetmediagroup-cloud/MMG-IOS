import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),source=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/readiness-certificate-succession.js"),"utf8"),css=readFileSync(join(repoRoot,"web/kairos-dashboard/styles/readiness-certificate-succession.css"),"utf8");assert.ok(index.includes("scripts/readiness-certificate-succession.js"),"Certificate succession module is not loaded.");assert.ok(index.includes("styles/readiness-certificate-succession.css"),"Certificate succession stylesheet is not loaded.");for(const marker of["Certificate Succession","command-center-certificate-succession","Verify replacement certificate","Designate governing certificate","Preserve succession receipt","data-create-certificate-succession"])assert.ok(source.includes(marker),`Certificate succession is missing: ${marker}`);assert.ok(source.includes("approvalRequired:true"),"Certificate succession must require approval.");assert.ok(source.includes('priority:"critical"'),"Certificate succession must remain critical priority.");assert.ok(source.includes("historical evidence"),"Historical certificate preservation boundary is missing.");assert.ok(css.includes(".readiness-certificate-succession"),"Certificate succession styling is missing.");console.log(JSON.stringify({status:"ready",feature:"certificate-succession",approvalRequired:true,historyPreserved:true},null,2));