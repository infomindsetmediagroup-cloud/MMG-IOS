import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goals.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics-kpi-goals.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-kpi-goals-20260715-1','GOALS_KEY','Set goal','Progress toward targets','At risk','On track','Achieved','role="progressbar"','getGoals','setGoal','clearGoal'])assert.ok(ui.includes(marker),`Shopify KPI goals missing ${marker}`);for(const marker of['.shopify-kpi-goals','.shopify-kpi-goal-track','data-state=achieved','data-state=at-risk'])assert.ok(css.includes(marker),`Shopify KPI goals style missing ${marker}`);assert.ok(index.includes('shopify-analytics-kpi-goals.js'),'KPI goals module must load after Shopify analytics');assert.ok(index.indexOf('shopify-analytics.js')<index.indexOf('shopify-analytics-kpi-goals.js'),'KPI goals must load after Shopify analytics');assert.ok(!index.includes('scripts/live-analytics.js'),'internal live analytics must remain excluded');console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-goals',savedTargets:true,progress:true,statuses:['at-risk','on-track','achieved'],deviceLocal:true,observationalOnly:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));