import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),edge=readFileSync(join(root,'src/kairos-production-entry-v29.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/live-analytics.css'),'utf8');for(const marker of['/api/analytics/live/history','HISTORY_LIMIT=96','appendHistory','buildAlerts','buildTrends','canonical-live-analytics-history','historyWindow:HISTORY_LIMIT'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['kairos-live-analytics-ui-20260715-3','active analytics alert','History window','data-trend','trends.shopify','trends.readiness','getSnapshot'])assert.ok(ui.includes(marker),`UI missing ${marker}`);for(const marker of['.live-analytics-alerts','data-severity=critical','data-trend=up','.live-analytics-foot'])assert.ok(css.includes(marker),`CSS missing ${marker}`);console.log(JSON.stringify({status:'ready',feature:'live-analytics-history-alerts',historySnapshots:96,trendDeltas:true,anomalyAlerts:true,browser:true,appBridge:true,alertLifecycleCompatible:true,reportingCompatible:true},null,2));