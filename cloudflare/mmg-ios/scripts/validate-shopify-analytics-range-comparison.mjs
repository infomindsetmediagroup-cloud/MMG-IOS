import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-analytics-20260715-6','Today','7 Days','30 Days','90 Days','range=${encodeURIComponent(range)}','compare=previous','X-Kairos-Analytics-Range','localStorage.setItem(RANGE_KEY','Since last refresh','Previous period','window.KairosShopifyAnalytics'])assert.ok(ui.includes(marker),`Shopify analytics UI missing ${marker}`);for(const marker of['.shopify-analytics-ranges','aria-pressed=true','.shopify-analytics-value','data-direction=up','data-direction=down'])assert.ok(css.includes(marker),`Shopify analytics CSS missing ${marker}`);assert.ok(index.includes('shopify-analytics.js'),'Shopify analytics must remain mounted in the main command center');assert.ok(!index.includes('scripts/live-analytics.js'),'internal live analytics must remain excluded from the main shell');assert.ok(!ui.startsWith("import'./live-analytics.js'"),'Shopify analytics must remain standalone');console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-range-comparison',ranges:['today','7d','30d','90d'],selectionPersistent:true,previousPeriodRequest:true,snapshotFallbackComparison:true,refreshSeconds:15,shopifyVisible:true,internalLiveAnalyticsExcluded:true},null,2));