import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),source=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/readiness-recertification.js"),"utf8"),css=readFileSync(join(repoRoot,"web/kairos-dashboard/styles/readiness-recertification.css"),"utf8");assert.ok(index.includes("scripts/readiness-recertification.js"),"Recertification module is not loaded.");assert.ok(index.includes("styles/readiness-recertification.css"),"Recertification stylesheet is not loaded.");for(const marker of["System Recertification","command-center-kairos-recertification","Reconcile applicability decision","Approve and preserve new certificate","data-create-recertification"])assert.ok(source.includes(marker),`Recertification is missing: ${marker}`);assert.ok(source.includes("approvalRequired:true"),"Recertification must require approval.");assert.ok(source.includes('priority:"critical"'),"Recertification must remain critical priority.");assert.ok(css.includes(".readiness-recertification"),"Recertification styling is missing.");console.log(JSON.stringify({status:"ready",feature:"kairos-system-recertification",approvalRequired:true,historicalCertificatePreserved:true},null,2));