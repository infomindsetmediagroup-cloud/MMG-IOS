import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),source=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/readiness-recovery-verification.js"),"utf8"),css=readFileSync(join(repoRoot,"web/kairos-dashboard/styles/readiness-recovery-verification.css"),"utf8");assert.ok(index.includes("scripts/readiness-recovery-verification.js"),"Recovery verification module is not loaded.");assert.ok(index.includes("styles/readiness-recovery-verification.css"),"Recovery verification stylesheet is not loaded.");for(const marker of["Recovery Verification","command-center-recovery-verification","Verify remediation closure","Verify runtime recovery","Preserve recovery disposition","approvalRequired:true"])assert.ok(source.includes(marker),`Recovery verification is missing: ${marker}`);assert.ok(css.includes(".readiness-recovery-verification"),"Recovery verification styling is missing.");console.log(JSON.stringify({status:"ready",feature:"operational-recovery-verification",approvalRequired:true},null,2));