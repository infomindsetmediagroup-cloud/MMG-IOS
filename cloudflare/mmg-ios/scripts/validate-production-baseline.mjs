import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,'..'),repo=resolve(root,'../..'),src=join(root,'src'),entry=join(src,'kairos-production-entry-v6.js'),engine=join(src,'kairos-deliverable-acceptance-v1.js'),index=join(repo,'web/kairos-dashboard/index.html'),ui=join(repo,'web/kairos-dashboard/scripts/deliverable-acceptance-loop.js');for(const f of[entry,engine,index,ui,join(src,'kairos-production-entry-v5.js')])assert.ok(existsSync(f),`Required deployment file missing: ${f}`);const wrangler=readFileSync(join(root,'wrangler.toml'),'utf8');assert.ok(wrangler.includes('main = "src/kairos-production-entry-v6.js"'));const edge=readFileSync(entry,'utf8');for(const m of['kairos-production-entry-v5.js','recordAcceptance','approveResolution','linkSuccessor','/api/deliverable-acceptances','async scheduled','runtime.scheduled','X-Kairos-Deliverable-Acceptance'])assert.ok(edge.includes(m),`Active edge missing ${m}`);const acceptance=readFileSync(engine,'utf8');for(const m of['deliveredVersionImmutable:true','approvalRequiredForResolution','successor-linked','refundAutomatic:false','portalReplacementAutomatic:false'])assert.ok(acceptance.includes(m),`Acceptance engine missing ${m}`);assert.ok(readFileSync(index,'utf8').includes('deliverable-acceptance-loop.js'));assert.ok(readFileSync(ui,'utf8').includes('Customer Acceptance and Revision Lineage'));console.log(JSON.stringify({status:'ready',baseline:'kairos-production-baseline-20260714-14',activeEntry:'kairos-production-entry-v6',delegationPreserved:true,scheduledHandlers:true,deliverableAcceptance:true},null,2));