import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const growth=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/growth-plan.js"),"utf8"),campaign=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/campaign-operations.js"),"utf8"),index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),registry=readFileSync(join(workerRoot,"src/kairos-readiness-registry-v1.js"),"utf8");for(const marker of["Growth Operations","Next best action","Build Campaign","Open My Work","kairos:campaign-operations:open"])assert.ok(growth.includes(marker),`Growth operations missing: ${marker}`);for(const marker of["Campaign Operations","Next best action","Send Results to Growth Plan","Open My Work","kairos:growth-plan:open"])assert.ok(campaign.includes(marker),`Campaign operations missing: ${marker}`);assert.ok(index.includes("recovery-20260714-35"),"Browser batch marker missing.");assert.ok(registry.includes('"growth-plan":65'),"Growth readiness floor missing.");assert.ok(registry.includes('"campaign-operations":60'),"Campaign readiness floor missing.");assert.ok(!growth.includes("position:fixed")&&!campaign.includes("position:fixed"),"Batch must not add floating controls.");console.log(JSON.stringify({status:"ready",feature:"business-growth-campaign-batch",growthOperations:true,campaignOperations:true,twoWayHandoff:true,simplifiedMyWork:true,businessReadiness:61},null,2));