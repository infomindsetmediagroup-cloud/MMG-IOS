import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const growth=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/growth-plan.js"),"utf8"),campaign=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/campaign-operations.js"),"utf8"),index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),registry=readFileSync(join(workerRoot,"src/kairos-readiness-registry-v1.js"),"utf8");for(const marker of["Growth Operations","Next best action","Build Campaign","Open My Work","kairos:campaign-operations:open"])assert.ok(growth.includes(marker));for(const marker of["Campaign Command Workspace","Next best action","Measure and Close Campaign","Build Next Growth Experiment","Open in Work Queue","kairos:growth-plan:open"])assert.ok(campaign.includes(marker));assert.ok(index.includes("scripts/growth-plan.js")&&index.includes("scripts/campaign-operations.js"));assert.ok(registry.includes('"growth-plan":78'));assert.ok(registry.includes('"campaign-operations":78'));assert.ok(!growth.includes("position:fixed")&&!campaign.includes("position:fixed"));console.log(JSON.stringify({status:"ready",feature:"business-growth-campaign-closed-loop",growthReadiness:78,campaignReadiness:78,canonicalReadiness:true},null,2));