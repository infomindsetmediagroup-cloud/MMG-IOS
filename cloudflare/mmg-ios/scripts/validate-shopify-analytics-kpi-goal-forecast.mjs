import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-forecast.js'),'utf8'),history=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics-kpi-goal-history.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics-kpi-goal-forecast.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-kpi-goal-forecast-20260715-1','Projected outcome','Progress pace','Projected completion','Forecast status','Confidence','Required pace','Checkpoint span','on-pace','off-pace','getForecast','calculate'])assert.ok(ui.includes(marker),`Shopify KPI forecast missing ${marker}`);for(const marker of['.shopify-kpi-goal-forecast','.shopify-kpi-forecast-grid','.shopify-kpi-forecast-meter','data-forecast-status=on-pace','data-forecast-status=off-pace'])assert.ok(css.includes(marker),`Shopify KPI forecast style missing ${marker}`);assert.ok(history.startsWith("import'./shopify-analytics-kpi-goal-forecast.js'"),'Forecast must load with goal history');assert.ok(index.includes('shopify-analytics-kpi-goals.js'));assert.ok(!index.includes('scripts/live-analytics.js'));console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-goal-forecast',pace:true,projectedCompletion:true,confidence:true,requiredPace:true,observationalOnly:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));