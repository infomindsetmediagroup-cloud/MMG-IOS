import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const here=dirname(fileURLToPath(import.meta.url)),root=resolve(here,'..'),repo=resolve(root,'../..');const api=readFileSync(join(root,'src/kairos-readiness-registry-v1.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/readiness-registry.js'),'utf8'),hub=readFileSync(join(repo,'web/kairos-dashboard/scripts/command-hub.js'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-readiness-registry-20260714-11','oneHundredReservedForBlueprintCompletion:true','production-verified','buildSummary','baselineMigrationFloor:true','campaign-operations":78','support-intelligence":82'])assert.ok(api.includes(marker),`registry API missing ${marker}`);for(const marker of['/api/readiness-registry','MutationObserver','production readiness','Readiness Truth','100% remains reserved','setInterval(loadRegistry,15000)','card-signal'])assert.ok(ui.includes(marker),`registry UI missing ${marker}`);assert.ok(index.includes('scripts/readiness-registry.js'));assert.ok(hub.includes('const readinessRegistry = {'),'legacy fallback table should remain identifiable until command hub modularization');assert.ok(ui.includes('applyRegistryToVisibleCards'));console.log(JSON.stringify({status:'ready',canonicalRegistry:true,liveGaugeReconciliation:true,evidenceVisible:true,legacyGaugeOverridden:true,automaticPromotion:false,blueprintCompletionReserved:true},null,2));