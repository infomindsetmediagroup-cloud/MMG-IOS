import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url)),workerRoot=resolve(here,".."),repoRoot=resolve(workerRoot,"../..");const offer=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/offer-builder.js"),"utf8"),launch=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/product-launch-studio.js"),"utf8"),portfolio=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/offer-portfolio-closeout.js"),"utf8"),launchPortfolio=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/product-launch-portfolio-closeout.js"),"utf8"),index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8"),registry=readFileSync(join(workerRoot,"src/kairos-readiness-registry-v1.js"),"utf8");for(const marker of["Offer Operations","Next best action","Build Product Launch","Open My Work","kairos:product-launch:open"])assert.ok(offer.includes(marker));for(const marker of["Product Launch Operations","Next best action","Refine Offer","Open My Work","kairos:offer-builder:open","Review Launch Portfolio"])assert.ok(launch.includes(marker));assert.ok(portfolio.includes('Verified Offer Portfolio Review'));assert.ok(launchPortfolio.includes('Product Launch Portfolio Closeout'));assert.ok(index.includes("recovery-20260714-34"));assert.ok(registry.includes('"product-launch":86'));assert.ok(registry.includes('"offer-builder":86'));assert.ok(!offer.includes("setInterval(")&&!launch.includes("setInterval("));console.log(JSON.stringify({status:"ready",batch:"business-offer-launch",offerBuilder:86,productLaunch:86,offerPortfolioCloseout:true,launchPortfolioCloseout:true,canonicalReadiness:true},null,2));