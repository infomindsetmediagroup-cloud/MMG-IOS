import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),engine=readFileSync(join(root,'src/kairos-live-analytics-destination-registry-v1.js'),'utf8'),edge=readFileSync(join(root,'src/kairos-production-entry-v30.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics-destination-registry.js'),'utf8'),handoff=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics-digest-delivery.js'),'utf8'),baseline=JSON.parse(readFileSync(join(root,'production-baseline.json'),'utf8')),wrangler=readFileSync(join(root,'wrangler.toml'),'utf8');for(const marker of['readDestinations','createDestination','updateDestination','destinationSummary','resolveVerifiedDestination','authorizationRequired:true','verificationRequired:true','automaticExternalDelivery:false','secretStorageProhibited:true','draft','verified','suspended','retired','healthy','degraded'])assert.ok(engine.includes(marker),`engine missing ${marker}`);for(const marker of['/api/analytics/live/destinations','verify|check|suspend|retire','deliveryDestinations','X-Kairos-Destination-Registry','resolveVerifiedDestination'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['Delivery destination registry','Register destination','Authorization evidence','Health check','Suspend','Retire'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(handoff.startsWith("import'./live-analytics-destination-registry.js'"));for(const marker of['destinationID','verified','healthy','approved destinations'])assert.ok(handoff.includes(marker),`handoff UI missing ${marker}`);assert.equal(baseline.baseline,'kairos-production-standard-20260715-10');assert.equal(baseline.worker.entry,'src/kairos-production-entry-v30.js');assert.ok(wrangler.includes('main = "src/kairos-production-entry-v30.js"'));console.log(JSON.stringify({status:'ready',feature:'live-analytics-destination-registry',ownership:true,authorization:true,verification:true,healthLifecycle:true,suspension:true,retirement:true,registeredDestinationRequired:true,automaticExternalDelivery:false,activeEdge:'v30',baselinePreserved:true,browser:true,appBridge:true},null,2));