import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ops=readFileSync(join(root,'src/kairos-live-analytics-operations-v1.js'),'utf8'),edge=readFileSync(join(root,'src/kairos-production-entry-v29.js'),'utf8'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/live-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/live-analytics.css'),'utf8');for(const marker of['applyAlertLifecycle','updateAlertReceipt','readAlertReceipts','analyticsReportResponse','acknowledge','resolve','reopen','text/csv','observationalOnly:true'])assert.ok(ops.includes(marker),`operations missing ${marker}`);for(const marker of['/api/analytics/live/alerts','/api/analytics/live/report','alertSummary','reportFormats','canonical-live-analytics-alert-lifecycle'])assert.ok(edge.includes(marker),`edge missing ${marker}`);for(const marker of['Export JSON','Export CSV','data-alert-action','Acknowledge','Resolve','Reopen','exportReport'])assert.ok(ui.includes(marker),`UI missing ${marker}`);assert.ok(css.includes('data-status=resolved')&&css.includes('.live-analytics-alerts button'));console.log(JSON.stringify({status:'ready',features:['analytics-alert-lifecycle','analytics-report-export'],acknowledge:true,resolve:true,reopen:true,json:true,csv:true,automaticMutation:false,browser:true,appBridge:true},null,2));