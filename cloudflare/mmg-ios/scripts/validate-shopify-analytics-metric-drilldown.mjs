import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-analytics-20260715-8','selectedMetricID','renderDrilldown','data-shopify-metric','data-close-shopify-drilldown','Current period','Previous period','Absolute change','Percentage change','Comparison source','Data source','Metric status','Last successful synchronization'])assert.ok(ui.includes(marker),`Shopify analytics drilldown missing ${marker}`);for(const marker of['shopify-analytics-drilldown','shopify-analytics-drilldown-grid','aria-pressed=true','focus-visible'])assert.ok(css.includes(marker),`Shopify analytics drilldown style missing ${marker}`);assert.ok(index.includes('shopify-analytics.js'));assert.ok(!index.includes('scripts/live-analytics.js'));assert.ok(!ui.startsWith("import'./live-analytics.js'"));console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-metric-drilldown',currentPeriod:true,previousPeriod:true,absoluteChange:true,percentageChange:true,provenance:true,synchronizationReceipt:true,mobileResponsive:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));