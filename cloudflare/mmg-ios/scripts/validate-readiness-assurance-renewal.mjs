import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{dirname,join,resolve}from"node:path";import{fileURLToPath}from"node:url";const here=dirname(fileURLToPath(import.meta.url));const workerRoot=resolve(here,"..");const repoRoot=resolve(workerRoot,"../..");const index=readFileSync(join(repoRoot,"web/kairos-dashboard/index.html"),"utf8");const source=readFileSync(join(repoRoot,"web/kairos-dashboard/scripts/readiness-assurance-renewal.js"),"utf8");const css=readFileSync(join(repoRoot,"web/kairos-dashboard/styles/readiness-assurance-renewal.css"),"utf8");assert.ok(index.includes("scripts/readiness-assurance-renewal.js"),"Assurance renewal module is not loaded.");assert.ok(index.includes("styles/readiness-assurance-renewal.css"),"Assurance renewal stylesheet is not loaded.");for(const marker of["Assurance Renewal","command-center-assurance-renewal","Reconcile recovery receipt","Preserve renewed assurance receipt","data-create-assurance-renewal"]){assert.ok(source.includes(marker),`Assurance renewal is missing: ${marker}`)}assert.ok(source.includes("approvalRequired:true"),"Assurance renewal must require approval.");assert.ok(source.includes('priority:"critical"'),"Assurance renewal must remain critical priority.");assert.ok(!source.includes("MutationObserver"),"Assurance renewal must not add a MutationObserver.");assert.ok(!source.includes("setInterval"),"Assurance renewal must not add polling.");assert.ok(css.includes(".readiness-assurance-renewal"),"Assurance renewal styling is missing.");console.log(JSON.stringify({status:"ready",feature:"kairos-operational-assurance-renewal",approvalRequired:true},null,2));