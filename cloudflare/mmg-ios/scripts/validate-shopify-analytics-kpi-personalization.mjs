import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{dirname,join,resolve}from'node:path';import{fileURLToPath}from'node:url';const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),repo=resolve(root,'../..'),ui=readFileSync(join(repo,'web/kairos-dashboard/scripts/shopify-analytics.js'),'utf8'),css=readFileSync(join(repo,'web/kairos-dashboard/styles/shopify-analytics.css'),'utf8'),index=readFileSync(join(repo,'web/kairos-dashboard/index.html'),'utf8');for(const marker of['kairos-shopify-analytics-20260715-8','LAYOUT_KEY','Customize KPIs','renderCustomizer','togglePin','toggleVisibility','moveMetric','resetLayout','readLayout','saveLayout','getLayout','Pinned KPI'])assert.ok(ui.includes(marker),`Shopify KPI personalization missing ${marker}`);for(const marker of['.shopify-kpi-customizer','.shopify-kpi-actions','shopify-analytics-empty'])assert.ok(css.includes(marker),`Shopify KPI personalization style missing ${marker}`);assert.ok(index.includes('shopify-analytics.js'));assert.ok(!index.includes('scripts/live-analytics.js'));assert.ok(!ui.startsWith("import'./live-analytics.js'"));console.log(JSON.stringify({status:'ready',feature:'shopify-analytics-kpi-personalization',pinning:true,reordering:true,visibility:true,persistentLayout:true,resetDefault:true,minimumVisibleMetric:true,mobileResponsive:true,shopifyPanelVisible:true,internalAnalyticsExcluded:true},null,2));